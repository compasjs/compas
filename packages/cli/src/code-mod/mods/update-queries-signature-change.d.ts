/**
 * Convert `queries.entitySelect(sql, { ...where })` to `queryEntity({ where: { ...where
 * } }).exec(sql)`
 *
 * @param {Logger} logger
 * @returns {Promise<void>}
 */
export function executeUpdateQueriesSignatureChange(
  logger: Logger,
): Promise<void>;
//# sourceMappingURL=update-queries-signature-change.d.ts.map
