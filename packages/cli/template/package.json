{
  "name": "{{name}}",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "NODE_ENV=production node ./scripts/api.js"
  },
  "dependencies": {
    "@compas/server": "{{version}}",
    "@compas/stdlib": "{{version}}",
    "@compas/store": "{{version}}"
  },
  "devDependencies": {
    "@compas/cli": "{{version}}",
    "@compas/code-gen": "{{version}}",
    "@compas/lint-config": "{{version}}",
    "axios": "0.21.1",
    "axios-cookiejar-support": "1.0.1",
    "tough-cookie": "4.0.0"
  },
  "prettier": "@compas/lint-config/prettierrc",
  "engines": {
    "node": ">=16"
  }
}
