/* eslint-disable no-unused-vars */

{{= routerImports({ routes }) }}
{{= routerTags({ tags }) }}

{{= routerHandlers({ routes }) }}

{{= routerRouteFn({ routeTrie }) }}

module.exports = {
 routeHandlers,
 router,
 taggedMiddleware,
};
