// Generated by @lbu/code-gen
/* eslint-disable no-unused-vars */

export const validatorHooks = {};

const isNil = (value) => value === undefined || value === null;

/**
 * This function should not throw as the corresponding validator will do that
 * @callback ValidationErrorFn
 * @param {string} key
 * @param {Object} info
 */

/**
 * @type ValidationErrorFn
 */
let _errorBuildFn = (key, info) => {
  const err = new Error(`ValidationError: ${key}`);
  err.key = key;
  err.info = info;
  return err;
};

/**
 * Set a different error function, for example AppError.validationError
 * @param {ValidationErrorFn} fn
 */
export function validatorSetError(fn) {
  _errorBuildFn = fn;
}

function buildError(key, info) {
  throw _errorBuildFn(key, info);
}

export const codeGenValidators = {
  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { CodeGenAnyType}
   */
  anyType: (value, propertyPath = "$") => {
    let result = value;
    if ("preValidateCodeGenAnyType" in validatorHooks) {
      result = validatorHooks["preValidateCodeGenAnyType"](result);
    }
    result = objectValidator0(result, propertyPath);
    if ("postValidateCodeGenAnyType" in validatorHooks) {
      return validatorHooks["postValidateCodeGenAnyType"](result);
    }
    return result;
  },

  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { CodeGenAnyOfType}
   */
  anyOfType: (value, propertyPath = "$") => {
    let result = value;
    if ("preValidateCodeGenAnyOfType" in validatorHooks) {
      result = validatorHooks["preValidateCodeGenAnyOfType"](result);
    }
    result = objectValidator9(result, propertyPath);
    if ("postValidateCodeGenAnyOfType" in validatorHooks) {
      return validatorHooks["postValidateCodeGenAnyOfType"](result);
    }
    return result;
  },

  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { CodeGenArrayType}
   */
  arrayType: (value, propertyPath = "$") => {
    let result = value;
    if ("preValidateCodeGenArrayType" in validatorHooks) {
      result = validatorHooks["preValidateCodeGenArrayType"](result);
    }
    result = objectValidator13(result, propertyPath);
    if ("postValidateCodeGenArrayType" in validatorHooks) {
      return validatorHooks["postValidateCodeGenArrayType"](result);
    }
    return result;
  },

  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { CodeGenBooleanType}
   */
  booleanType: (value, propertyPath = "$") => {
    let result = value;
    if ("preValidateCodeGenBooleanType" in validatorHooks) {
      result = validatorHooks["preValidateCodeGenBooleanType"](result);
    }
    result = objectValidator18(result, propertyPath);
    if ("postValidateCodeGenBooleanType" in validatorHooks) {
      return validatorHooks["postValidateCodeGenBooleanType"](result);
    }
    return result;
  },

  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { CodeGenDateType}
   */
  dateType: (value, propertyPath = "$") => {
    let result = value;
    if ("preValidateCodeGenDateType" in validatorHooks) {
      result = validatorHooks["preValidateCodeGenDateType"](result);
    }
    result = objectValidator22(result, propertyPath);
    if ("postValidateCodeGenDateType" in validatorHooks) {
      return validatorHooks["postValidateCodeGenDateType"](result);
    }
    return result;
  },

  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { CodeGenFileType}
   */
  fileType: (value, propertyPath = "$") => {
    let result = value;
    if ("preValidateCodeGenFileType" in validatorHooks) {
      result = validatorHooks["preValidateCodeGenFileType"](result);
    }
    result = objectValidator24(result, propertyPath);
    if ("postValidateCodeGenFileType" in validatorHooks) {
      return validatorHooks["postValidateCodeGenFileType"](result);
    }
    return result;
  },

  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { CodeGenGenericType}
   */
  genericType: (value, propertyPath = "$") => {
    let result = value;
    if ("preValidateCodeGenGenericType" in validatorHooks) {
      result = validatorHooks["preValidateCodeGenGenericType"](result);
    }
    result = objectValidator26(result, propertyPath);
    if ("postValidateCodeGenGenericType" in validatorHooks) {
      return validatorHooks["postValidateCodeGenGenericType"](result);
    }
    return result;
  },

  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { CodeGenNumberType}
   */
  numberType: (value, propertyPath = "$") => {
    let result = value;
    if ("preValidateCodeGenNumberType" in validatorHooks) {
      result = validatorHooks["preValidateCodeGenNumberType"](result);
    }
    result = objectValidator30(result, propertyPath);
    if ("postValidateCodeGenNumberType" in validatorHooks) {
      return validatorHooks["postValidateCodeGenNumberType"](result);
    }
    return result;
  },

  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { CodeGenObjectType}
   */
  objectType: (value, propertyPath = "$") => {
    let result = value;
    if ("preValidateCodeGenObjectType" in validatorHooks) {
      result = validatorHooks["preValidateCodeGenObjectType"](result);
    }
    result = objectValidator34(result, propertyPath);
    if ("postValidateCodeGenObjectType" in validatorHooks) {
      return validatorHooks["postValidateCodeGenObjectType"](result);
    }
    return result;
  },

  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { CodeGenReferenceType}
   */
  referenceType: (value, propertyPath = "$") => {
    let result = value;
    if ("preValidateCodeGenReferenceType" in validatorHooks) {
      result = validatorHooks["preValidateCodeGenReferenceType"](result);
    }
    result = objectValidator40(result, propertyPath);
    if ("postValidateCodeGenReferenceType" in validatorHooks) {
      return validatorHooks["postValidateCodeGenReferenceType"](result);
    }
    return result;
  },

  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { CodeGenRelationType}
   */
  relationType: (value, propertyPath = "$") => {
    let result = value;
    if ("preValidateCodeGenRelationType" in validatorHooks) {
      result = validatorHooks["preValidateCodeGenRelationType"](result);
    }
    result = objectValidator43(result, propertyPath);
    if ("postValidateCodeGenRelationType" in validatorHooks) {
      return validatorHooks["postValidateCodeGenRelationType"](result);
    }
    return result;
  },

  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { CodeGenStringType}
   */
  stringType: (value, propertyPath = "$") => {
    let result = value;
    if ("preValidateCodeGenStringType" in validatorHooks) {
      result = validatorHooks["preValidateCodeGenStringType"](result);
    }
    result = objectValidator48(result, propertyPath);
    if ("postValidateCodeGenStringType" in validatorHooks) {
      return validatorHooks["postValidateCodeGenStringType"](result);
    }
    return result;
  },

  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { CodeGenUuidType}
   */
  uuidType: (value, propertyPath = "$") => {
    let result = value;
    if ("preValidateCodeGenUuidType" in validatorHooks) {
      result = validatorHooks["preValidateCodeGenUuidType"](result);
    }
    result = objectValidator53(result, propertyPath);
    if ("postValidateCodeGenUuidType" in validatorHooks) {
      return validatorHooks["postValidateCodeGenUuidType"](result);
    }
    return result;
  },

  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { CodeGenRouteType}
   */
  routeType: (value, propertyPath = "$") => {
    let result = value;
    if ("preValidateCodeGenRouteType" in validatorHooks) {
      result = validatorHooks["preValidateCodeGenRouteType"](result);
    }
    result = objectValidator55(result, propertyPath);
    if ("postValidateCodeGenRouteType" in validatorHooks) {
      return validatorHooks["postValidateCodeGenRouteType"](result);
    }
    return result;
  },

  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { CodeGenType}
   */
  type: (value, propertyPath = "$") => {
    let result = value;
    if ("preValidateCodeGenType" in validatorHooks) {
      result = validatorHooks["preValidateCodeGenType"](result);
    }
    result = anyOfValidator64(result, propertyPath);
    if ("postValidateCodeGenType" in validatorHooks) {
      return validatorHooks["postValidateCodeGenType"](result);
    }
    return result;
  },

  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { CodeGenStructure}
   */
  structure: (value, propertyPath = "$") => {
    let result = value;
    if ("preValidateCodeGenStructure" in validatorHooks) {
      result = validatorHooks["preValidateCodeGenStructure"](result);
    }
    result = genericValidator79(result, propertyPath);
    if ("postValidateCodeGenStructure" in validatorHooks) {
      return validatorHooks["postValidateCodeGenStructure"](result);
    }
    return result;
  },

  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { CodeGenTemplateState}
   */
  templateState: (value, propertyPath = "$") => {
    let result = value;
    if ("preValidateCodeGenTemplateState" in validatorHooks) {
      result = validatorHooks["preValidateCodeGenTemplateState"](result);
    }
    result = objectValidator82(result, propertyPath);
    if ("postValidateCodeGenTemplateState" in validatorHooks) {
      return validatorHooks["postValidateCodeGenTemplateState"](result);
    }
    return result;
  },

  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { CodeGenTemplateImports}
   */
  templateImports: (value, propertyPath = "$") => {
    let result = value;
    if ("preValidateCodeGenTemplateImports" in validatorHooks) {
      result = validatorHooks["preValidateCodeGenTemplateImports"](result);
    }
    result = objectValidator85(result, propertyPath);
    if ("postValidateCodeGenTemplateImports" in validatorHooks) {
      return validatorHooks["postValidateCodeGenTemplateImports"](result);
    }
    return result;
  },
};

function stringValidator1(
  value,
  propertyPath,
  errorList = [],
  parentType = "string",
) {
  if (isNil(value)) {
    return buildError(
      "validator.string.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "string") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (value.length < 1) {
    const min = 1;
    return buildError(
      `validator.${parentType}.min`,
      { propertyPath, min },
      errorList,
    );
  }
  if (value !== "any") {
    const oneOf = "any";
    return buildError(
      `validator.${parentType}.oneOf`,
      { propertyPath, oneOf },
      errorList,
    );
  }
  return value;
}
function stringValidator2(
  value,
  propertyPath,
  errorList = [],
  parentType = "string",
) {
  if (isNil(value)) {
    return;
  }
  if (typeof value !== "string") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (value.length < 0) {
    const min = 0;
    return buildError(
      `validator.${parentType}.min`,
      { propertyPath, min },
      errorList,
    );
  }
  return value;
}
function booleanValidator3(
  value,
  propertyPath,
  errorList = [],
  parentType = "boolean",
) {
  if (isNil(value)) {
    return false;
  }
  if (typeof value !== "boolean") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  return value;
}
function stringValidator5(
  value,
  propertyPath,
  errorList = [],
  parentType = "string",
) {
  if (isNil(value)) {
    return buildError(
      "validator.string.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "string") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (value.length < 0) {
    const min = 0;
    return buildError(
      `validator.${parentType}.min`,
      { propertyPath, min },
      errorList,
    );
  }
  return value;
}
function booleanValidator6(
  value,
  propertyPath,
  errorList = [],
  parentType = "boolean",
) {
  if (isNil(value)) {
    return buildError(
      "validator.boolean.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "boolean") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  return value;
}
function numberValidator7(
  value,
  propertyPath,
  errorList = [],
  parentType = "number",
) {
  if (isNil(value)) {
    return buildError(
      "validator.number.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "number" || isNaN(value) || !isFinite(value)) {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (!Number.isInteger(value)) {
    return buildError(
      `validator.${parentType}.integer`,
      { propertyPath },
      errorList,
    );
  }
  return value;
}
function anyOfValidator4(
  value,
  propertyPath,
  errorList = [],
  parentType = "anyOf",
) {
  if (isNil(value)) {
    return;
  }
  const errors = [];
  try {
    return stringValidator5(value, propertyPath, errorList);
  } catch (e) {
    errors.push({
      name: e.name,
      message: e.message,
      key: e.key,
      info: e.info,
    });
  }
  try {
    return booleanValidator6(value, propertyPath, errorList);
  } catch (e) {
    errors.push({
      name: e.name,
      message: e.message,
      key: e.key,
      info: e.info,
    });
  }
  try {
    return numberValidator7(value, propertyPath, errorList);
  } catch (e) {
    errors.push({
      name: e.name,
      message: e.message,
      key: e.key,
      info: e.info,
    });
  }
  return buildError(
    `validator.${parentType}.type`,
    { propertyPath, errors },
    errorList,
  );
}
function stringValidator8(
  value,
  propertyPath,
  errorList = [],
  parentType = "string",
) {
  if (isNil(value)) {
    return;
  }
  if (typeof value !== "string") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (value.length < 1) {
    const min = 1;
    return buildError(
      `validator.${parentType}.min`,
      { propertyPath, min },
      errorList,
    );
  }
  return value;
}
function objectValidator0(
  value,
  propertyPath,
  errorList = [],
  parentType = "object",
) {
  if (isNil(value)) {
    return buildError(
      "validator.object.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  result["type"] = stringValidator1(
    value["type"],
    `${propertyPath}.` + `type`,
    errorList,
  );
  result["docString"] = stringValidator2(
    value["docString"],
    `${propertyPath}.` + `docString`,
    errorList,
  );
  result["isOptional"] = booleanValidator3(
    value["isOptional"],
    `${propertyPath}.` + `isOptional`,
    errorList,
  );
  result["defaultValue"] = anyOfValidator4(
    value["defaultValue"],
    `${propertyPath}.` + `defaultValue`,
    errorList,
  );
  result["uniqueName"] = stringValidator8(
    value["uniqueName"],
    `${propertyPath}.` + `uniqueName`,
    errorList,
  );
  result["group"] = stringValidator8(
    value["group"],
    `${propertyPath}.` + `group`,
    errorList,
  );
  result["name"] = stringValidator8(
    value["name"],
    `${propertyPath}.` + `name`,
    errorList,
  );
  result["typeOf"] = stringValidator8(
    value["typeOf"],
    `${propertyPath}.` + `typeOf`,
    errorList,
  );
  result["instanceOf"] = stringValidator8(
    value["instanceOf"],
    `${propertyPath}.` + `instanceOf`,
    errorList,
  );
  return result;
}
function stringValidator10(
  value,
  propertyPath,
  errorList = [],
  parentType = "string",
) {
  if (isNil(value)) {
    return buildError(
      "validator.string.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "string") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (value.length < 1) {
    const min = 1;
    return buildError(
      `validator.${parentType}.min`,
      { propertyPath, min },
      errorList,
    );
  }
  if (value !== "anyOf") {
    const oneOf = "anyOf";
    return buildError(
      `validator.${parentType}.oneOf`,
      { propertyPath, oneOf },
      errorList,
    );
  }
  return value;
}
function referenceValidator12(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return buildError(
      "validator.reference.undefined",
      { propertyPath },
      errorList,
    );
  }
  return codeGenValidators.type(value, propertyPath);
}
function arrayValidator11(
  value,
  propertyPath,
  errorList = [],
  parentType = "array",
) {
  if (isNil(value)) {
    return buildError("validator.array.undefined", { propertyPath }, errorList);
  }
  if (!Array.isArray(value)) {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = [];
  for (let i = 0; i < value.length; ++i) {
    result.push(
      referenceValidator12(value[i], `${propertyPath}[${i}]`, errorList),
    );
  }
  return result;
}
function objectValidator9(
  value,
  propertyPath,
  errorList = [],
  parentType = "object",
) {
  if (isNil(value)) {
    return buildError(
      "validator.object.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  result["type"] = stringValidator10(
    value["type"],
    `${propertyPath}.` + `type`,
    errorList,
  );
  result["docString"] = stringValidator2(
    value["docString"],
    `${propertyPath}.` + `docString`,
    errorList,
  );
  result["isOptional"] = booleanValidator3(
    value["isOptional"],
    `${propertyPath}.` + `isOptional`,
    errorList,
  );
  result["defaultValue"] = anyOfValidator4(
    value["defaultValue"],
    `${propertyPath}.` + `defaultValue`,
    errorList,
  );
  result["uniqueName"] = stringValidator8(
    value["uniqueName"],
    `${propertyPath}.` + `uniqueName`,
    errorList,
  );
  result["group"] = stringValidator8(
    value["group"],
    `${propertyPath}.` + `group`,
    errorList,
  );
  result["name"] = stringValidator8(
    value["name"],
    `${propertyPath}.` + `name`,
    errorList,
  );
  result["values"] = arrayValidator11(
    value["values"],
    `${propertyPath}.` + `values`,
    errorList,
  );
  return result;
}
function stringValidator14(
  value,
  propertyPath,
  errorList = [],
  parentType = "string",
) {
  if (isNil(value)) {
    return buildError(
      "validator.string.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "string") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (value.length < 1) {
    const min = 1;
    return buildError(
      `validator.${parentType}.min`,
      { propertyPath, min },
      errorList,
    );
  }
  if (value !== "array") {
    const oneOf = "array";
    return buildError(
      `validator.${parentType}.oneOf`,
      { propertyPath, oneOf },
      errorList,
    );
  }
  return value;
}
function numberValidator16(
  value,
  propertyPath,
  errorList = [],
  parentType = "number",
) {
  if (isNil(value)) {
    return;
  }
  if (typeof value !== "number" || isNaN(value) || !isFinite(value)) {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (!Number.isInteger(value)) {
    return buildError(
      `validator.${parentType}.integer`,
      { propertyPath },
      errorList,
    );
  }
  return value;
}
function objectValidator15(
  value,
  propertyPath,
  errorList = [],
  parentType = "object",
) {
  if (isNil(value)) {
    return buildError(
      "validator.object.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  result["convert"] = booleanValidator3(
    value["convert"],
    `${propertyPath}.` + `convert`,
    errorList,
  );
  result["min"] = numberValidator16(
    value["min"],
    `${propertyPath}.` + `min`,
    errorList,
  );
  result["max"] = numberValidator16(
    value["max"],
    `${propertyPath}.` + `max`,
    errorList,
  );
  return result;
}
function referenceValidator17(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return buildError(
      "validator.reference.undefined",
      { propertyPath },
      errorList,
    );
  }
  return codeGenValidators.type(value, propertyPath);
}
function objectValidator13(
  value,
  propertyPath,
  errorList = [],
  parentType = "object",
) {
  if (isNil(value)) {
    return buildError(
      "validator.object.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  result["type"] = stringValidator14(
    value["type"],
    `${propertyPath}.` + `type`,
    errorList,
  );
  result["docString"] = stringValidator2(
    value["docString"],
    `${propertyPath}.` + `docString`,
    errorList,
  );
  result["isOptional"] = booleanValidator3(
    value["isOptional"],
    `${propertyPath}.` + `isOptional`,
    errorList,
  );
  result["defaultValue"] = anyOfValidator4(
    value["defaultValue"],
    `${propertyPath}.` + `defaultValue`,
    errorList,
  );
  result["uniqueName"] = stringValidator8(
    value["uniqueName"],
    `${propertyPath}.` + `uniqueName`,
    errorList,
  );
  result["group"] = stringValidator8(
    value["group"],
    `${propertyPath}.` + `group`,
    errorList,
  );
  result["name"] = stringValidator8(
    value["name"],
    `${propertyPath}.` + `name`,
    errorList,
  );
  result["validator"] = objectValidator15(
    value["validator"],
    `${propertyPath}.` + `validator`,
    errorList,
  );
  result["values"] = referenceValidator17(
    value["values"],
    `${propertyPath}.` + `values`,
    errorList,
  );
  return result;
}
function stringValidator19(
  value,
  propertyPath,
  errorList = [],
  parentType = "string",
) {
  if (isNil(value)) {
    return buildError(
      "validator.string.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "string") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (value.length < 1) {
    const min = 1;
    return buildError(
      `validator.${parentType}.min`,
      { propertyPath, min },
      errorList,
    );
  }
  if (value !== "boolean") {
    const oneOf = "boolean";
    return buildError(
      `validator.${parentType}.oneOf`,
      { propertyPath, oneOf },
      errorList,
    );
  }
  return value;
}
function booleanValidator20(
  value,
  propertyPath,
  errorList = [],
  parentType = "boolean",
) {
  if (isNil(value)) {
    return;
  }
  if (typeof value !== "boolean") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  return value;
}
function objectValidator21(
  value,
  propertyPath,
  errorList = [],
  parentType = "object",
) {
  if (isNil(value)) {
    return buildError(
      "validator.object.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  result["convert"] = booleanValidator3(
    value["convert"],
    `${propertyPath}.` + `convert`,
    errorList,
  );
  return result;
}
function objectValidator18(
  value,
  propertyPath,
  errorList = [],
  parentType = "object",
) {
  if (isNil(value)) {
    return buildError(
      "validator.object.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  result["type"] = stringValidator19(
    value["type"],
    `${propertyPath}.` + `type`,
    errorList,
  );
  result["docString"] = stringValidator2(
    value["docString"],
    `${propertyPath}.` + `docString`,
    errorList,
  );
  result["isOptional"] = booleanValidator3(
    value["isOptional"],
    `${propertyPath}.` + `isOptional`,
    errorList,
  );
  result["defaultValue"] = anyOfValidator4(
    value["defaultValue"],
    `${propertyPath}.` + `defaultValue`,
    errorList,
  );
  result["uniqueName"] = stringValidator8(
    value["uniqueName"],
    `${propertyPath}.` + `uniqueName`,
    errorList,
  );
  result["group"] = stringValidator8(
    value["group"],
    `${propertyPath}.` + `group`,
    errorList,
  );
  result["name"] = stringValidator8(
    value["name"],
    `${propertyPath}.` + `name`,
    errorList,
  );
  result["oneOf"] = booleanValidator20(
    value["oneOf"],
    `${propertyPath}.` + `oneOf`,
    errorList,
  );
  result["validator"] = objectValidator21(
    value["validator"],
    `${propertyPath}.` + `validator`,
    errorList,
  );
  return result;
}
function stringValidator23(
  value,
  propertyPath,
  errorList = [],
  parentType = "string",
) {
  if (isNil(value)) {
    return buildError(
      "validator.string.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "string") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (value.length < 1) {
    const min = 1;
    return buildError(
      `validator.${parentType}.min`,
      { propertyPath, min },
      errorList,
    );
  }
  if (value !== "date") {
    const oneOf = "date";
    return buildError(
      `validator.${parentType}.oneOf`,
      { propertyPath, oneOf },
      errorList,
    );
  }
  return value;
}
function objectValidator22(
  value,
  propertyPath,
  errorList = [],
  parentType = "object",
) {
  if (isNil(value)) {
    return buildError(
      "validator.object.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  result["type"] = stringValidator23(
    value["type"],
    `${propertyPath}.` + `type`,
    errorList,
  );
  result["docString"] = stringValidator2(
    value["docString"],
    `${propertyPath}.` + `docString`,
    errorList,
  );
  result["isOptional"] = booleanValidator3(
    value["isOptional"],
    `${propertyPath}.` + `isOptional`,
    errorList,
  );
  result["defaultValue"] = anyOfValidator4(
    value["defaultValue"],
    `${propertyPath}.` + `defaultValue`,
    errorList,
  );
  result["uniqueName"] = stringValidator8(
    value["uniqueName"],
    `${propertyPath}.` + `uniqueName`,
    errorList,
  );
  result["group"] = stringValidator8(
    value["group"],
    `${propertyPath}.` + `group`,
    errorList,
  );
  result["name"] = stringValidator8(
    value["name"],
    `${propertyPath}.` + `name`,
    errorList,
  );
  return result;
}
function stringValidator25(
  value,
  propertyPath,
  errorList = [],
  parentType = "string",
) {
  if (isNil(value)) {
    return buildError(
      "validator.string.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "string") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (value.length < 1) {
    const min = 1;
    return buildError(
      `validator.${parentType}.min`,
      { propertyPath, min },
      errorList,
    );
  }
  if (value !== "file") {
    const oneOf = "file";
    return buildError(
      `validator.${parentType}.oneOf`,
      { propertyPath, oneOf },
      errorList,
    );
  }
  return value;
}
function objectValidator24(
  value,
  propertyPath,
  errorList = [],
  parentType = "object",
) {
  if (isNil(value)) {
    return buildError(
      "validator.object.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  result["type"] = stringValidator25(
    value["type"],
    `${propertyPath}.` + `type`,
    errorList,
  );
  result["docString"] = stringValidator2(
    value["docString"],
    `${propertyPath}.` + `docString`,
    errorList,
  );
  result["isOptional"] = booleanValidator3(
    value["isOptional"],
    `${propertyPath}.` + `isOptional`,
    errorList,
  );
  result["defaultValue"] = anyOfValidator4(
    value["defaultValue"],
    `${propertyPath}.` + `defaultValue`,
    errorList,
  );
  result["uniqueName"] = stringValidator8(
    value["uniqueName"],
    `${propertyPath}.` + `uniqueName`,
    errorList,
  );
  result["group"] = stringValidator8(
    value["group"],
    `${propertyPath}.` + `group`,
    errorList,
  );
  result["name"] = stringValidator8(
    value["name"],
    `${propertyPath}.` + `name`,
    errorList,
  );
  return result;
}
function stringValidator27(
  value,
  propertyPath,
  errorList = [],
  parentType = "string",
) {
  if (isNil(value)) {
    return buildError(
      "validator.string.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "string") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (value.length < 1) {
    const min = 1;
    return buildError(
      `validator.${parentType}.min`,
      { propertyPath, min },
      errorList,
    );
  }
  if (value !== "generic") {
    const oneOf = "generic";
    return buildError(
      `validator.${parentType}.oneOf`,
      { propertyPath, oneOf },
      errorList,
    );
  }
  return value;
}
function referenceValidator28(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return buildError(
      "validator.reference.undefined",
      { propertyPath },
      errorList,
    );
  }
  return codeGenValidators.type(value, propertyPath);
}
function referenceValidator29(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return buildError(
      "validator.reference.undefined",
      { propertyPath },
      errorList,
    );
  }
  return codeGenValidators.type(value, propertyPath);
}
function objectValidator26(
  value,
  propertyPath,
  errorList = [],
  parentType = "object",
) {
  if (isNil(value)) {
    return buildError(
      "validator.object.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  result["type"] = stringValidator27(
    value["type"],
    `${propertyPath}.` + `type`,
    errorList,
  );
  result["docString"] = stringValidator2(
    value["docString"],
    `${propertyPath}.` + `docString`,
    errorList,
  );
  result["isOptional"] = booleanValidator3(
    value["isOptional"],
    `${propertyPath}.` + `isOptional`,
    errorList,
  );
  result["defaultValue"] = anyOfValidator4(
    value["defaultValue"],
    `${propertyPath}.` + `defaultValue`,
    errorList,
  );
  result["uniqueName"] = stringValidator8(
    value["uniqueName"],
    `${propertyPath}.` + `uniqueName`,
    errorList,
  );
  result["group"] = stringValidator8(
    value["group"],
    `${propertyPath}.` + `group`,
    errorList,
  );
  result["name"] = stringValidator8(
    value["name"],
    `${propertyPath}.` + `name`,
    errorList,
  );
  result["keys"] = referenceValidator28(
    value["keys"],
    `${propertyPath}.` + `keys`,
    errorList,
  );
  result["values"] = referenceValidator29(
    value["values"],
    `${propertyPath}.` + `values`,
    errorList,
  );
  return result;
}
function stringValidator31(
  value,
  propertyPath,
  errorList = [],
  parentType = "string",
) {
  if (isNil(value)) {
    return buildError(
      "validator.string.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "string") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (value.length < 1) {
    const min = 1;
    return buildError(
      `validator.${parentType}.min`,
      { propertyPath, min },
      errorList,
    );
  }
  if (value !== "number") {
    const oneOf = "number";
    return buildError(
      `validator.${parentType}.oneOf`,
      { propertyPath, oneOf },
      errorList,
    );
  }
  return value;
}
function arrayValidator32(
  value,
  propertyPath,
  errorList = [],
  parentType = "array",
) {
  if (isNil(value)) {
    return;
  }
  if (!Array.isArray(value)) {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = [];
  for (let i = 0; i < value.length; ++i) {
    result.push(numberValidator7(value[i], `${propertyPath}[${i}]`, errorList));
  }
  return result;
}
function objectValidator33(
  value,
  propertyPath,
  errorList = [],
  parentType = "object",
) {
  if (isNil(value)) {
    return buildError(
      "validator.object.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  result["convert"] = booleanValidator3(
    value["convert"],
    `${propertyPath}.` + `convert`,
    errorList,
  );
  result["floatingPoint"] = booleanValidator3(
    value["floatingPoint"],
    `${propertyPath}.` + `floatingPoint`,
    errorList,
  );
  result["min"] = numberValidator16(
    value["min"],
    `${propertyPath}.` + `min`,
    errorList,
  );
  result["max"] = numberValidator16(
    value["max"],
    `${propertyPath}.` + `max`,
    errorList,
  );
  return result;
}
function objectValidator30(
  value,
  propertyPath,
  errorList = [],
  parentType = "object",
) {
  if (isNil(value)) {
    return buildError(
      "validator.object.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  result["type"] = stringValidator31(
    value["type"],
    `${propertyPath}.` + `type`,
    errorList,
  );
  result["docString"] = stringValidator2(
    value["docString"],
    `${propertyPath}.` + `docString`,
    errorList,
  );
  result["isOptional"] = booleanValidator3(
    value["isOptional"],
    `${propertyPath}.` + `isOptional`,
    errorList,
  );
  result["defaultValue"] = anyOfValidator4(
    value["defaultValue"],
    `${propertyPath}.` + `defaultValue`,
    errorList,
  );
  result["uniqueName"] = stringValidator8(
    value["uniqueName"],
    `${propertyPath}.` + `uniqueName`,
    errorList,
  );
  result["group"] = stringValidator8(
    value["group"],
    `${propertyPath}.` + `group`,
    errorList,
  );
  result["name"] = stringValidator8(
    value["name"],
    `${propertyPath}.` + `name`,
    errorList,
  );
  result["oneOf"] = arrayValidator32(
    value["oneOf"],
    `${propertyPath}.` + `oneOf`,
    errorList,
  );
  result["validator"] = objectValidator33(
    value["validator"],
    `${propertyPath}.` + `validator`,
    errorList,
  );
  return result;
}
function stringValidator35(
  value,
  propertyPath,
  errorList = [],
  parentType = "string",
) {
  if (isNil(value)) {
    return buildError(
      "validator.string.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "string") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (value.length < 1) {
    const min = 1;
    return buildError(
      `validator.${parentType}.min`,
      { propertyPath, min },
      errorList,
    );
  }
  if (value !== "object") {
    const oneOf = "object";
    return buildError(
      `validator.${parentType}.oneOf`,
      { propertyPath, oneOf },
      errorList,
    );
  }
  return value;
}
function objectValidator36(
  value,
  propertyPath,
  errorList = [],
  parentType = "object",
) {
  if (isNil(value)) {
    return buildError(
      "validator.object.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  result["strict"] = booleanValidator3(
    value["strict"],
    `${propertyPath}.` + `strict`,
    errorList,
  );
  return result;
}
function stringValidator38(
  value,
  propertyPath,
  errorList = [],
  parentType = "string",
) {
  if (isNil(value)) {
    return buildError(
      "validator.string.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "string") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (value.length < 1) {
    const min = 1;
    return buildError(
      `validator.${parentType}.min`,
      { propertyPath, min },
      errorList,
    );
  }
  return value;
}
function referenceValidator39(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return buildError(
      "validator.reference.undefined",
      { propertyPath },
      errorList,
    );
  }
  return codeGenValidators.type(value, propertyPath);
}
function genericValidator37(
  value,
  propertyPath,
  errorList = [],
  parentType = "generic",
) {
  if (isNil(value)) {
    return buildError(
      "validator.generic.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  for (const key of Object.keys(value)) {
    const validatedKey = stringValidator38(
      key,
      `${propertyPath}.$key[${key}]`,
      errorList,
    );
    const validatedValue = referenceValidator39(
      value[key],
      `${propertyPath}.$value[${key}]`,
      errorList,
    );
    result[validatedKey] = validatedValue;
  }
  return result;
}
function objectValidator34(
  value,
  propertyPath,
  errorList = [],
  parentType = "object",
) {
  if (isNil(value)) {
    return buildError(
      "validator.object.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  result["type"] = stringValidator35(
    value["type"],
    `${propertyPath}.` + `type`,
    errorList,
  );
  result["docString"] = stringValidator2(
    value["docString"],
    `${propertyPath}.` + `docString`,
    errorList,
  );
  result["isOptional"] = booleanValidator3(
    value["isOptional"],
    `${propertyPath}.` + `isOptional`,
    errorList,
  );
  result["defaultValue"] = anyOfValidator4(
    value["defaultValue"],
    `${propertyPath}.` + `defaultValue`,
    errorList,
  );
  result["uniqueName"] = stringValidator8(
    value["uniqueName"],
    `${propertyPath}.` + `uniqueName`,
    errorList,
  );
  result["group"] = stringValidator8(
    value["group"],
    `${propertyPath}.` + `group`,
    errorList,
  );
  result["name"] = stringValidator8(
    value["name"],
    `${propertyPath}.` + `name`,
    errorList,
  );
  result["validator"] = objectValidator36(
    value["validator"],
    `${propertyPath}.` + `validator`,
    errorList,
  );
  result["keys"] = genericValidator37(
    value["keys"],
    `${propertyPath}.` + `keys`,
    errorList,
  );
  return result;
}
function stringValidator41(
  value,
  propertyPath,
  errorList = [],
  parentType = "string",
) {
  if (isNil(value)) {
    return buildError(
      "validator.string.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "string") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (value.length < 1) {
    const min = 1;
    return buildError(
      `validator.${parentType}.min`,
      { propertyPath, min },
      errorList,
    );
  }
  if (value !== "reference") {
    const oneOf = "reference";
    return buildError(
      `validator.${parentType}.oneOf`,
      { propertyPath, oneOf },
      errorList,
    );
  }
  return value;
}
function objectValidator42(
  value,
  propertyPath,
  errorList = [],
  parentType = "object",
) {
  if (isNil(value)) {
    return buildError(
      "validator.object.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  result["uniqueName"] = stringValidator8(
    value["uniqueName"],
    `${propertyPath}.` + `uniqueName`,
    errorList,
  );
  result["group"] = stringValidator8(
    value["group"],
    `${propertyPath}.` + `group`,
    errorList,
  );
  result["name"] = stringValidator8(
    value["name"],
    `${propertyPath}.` + `name`,
    errorList,
  );
  return result;
}
function objectValidator40(
  value,
  propertyPath,
  errorList = [],
  parentType = "object",
) {
  if (isNil(value)) {
    return buildError(
      "validator.object.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  result["type"] = stringValidator41(
    value["type"],
    `${propertyPath}.` + `type`,
    errorList,
  );
  result["docString"] = stringValidator2(
    value["docString"],
    `${propertyPath}.` + `docString`,
    errorList,
  );
  result["isOptional"] = booleanValidator3(
    value["isOptional"],
    `${propertyPath}.` + `isOptional`,
    errorList,
  );
  result["defaultValue"] = anyOfValidator4(
    value["defaultValue"],
    `${propertyPath}.` + `defaultValue`,
    errorList,
  );
  result["uniqueName"] = stringValidator8(
    value["uniqueName"],
    `${propertyPath}.` + `uniqueName`,
    errorList,
  );
  result["group"] = stringValidator8(
    value["group"],
    `${propertyPath}.` + `group`,
    errorList,
  );
  result["name"] = stringValidator8(
    value["name"],
    `${propertyPath}.` + `name`,
    errorList,
  );
  result["reference"] = objectValidator42(
    value["reference"],
    `${propertyPath}.` + `reference`,
    errorList,
  );
  return result;
}
function stringValidator44(
  value,
  propertyPath,
  errorList = [],
  parentType = "string",
) {
  if (isNil(value)) {
    return buildError(
      "validator.string.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "string") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (value.length < 1) {
    const min = 1;
    return buildError(
      `validator.${parentType}.min`,
      { propertyPath, min },
      errorList,
    );
  }
  if (value !== "relation") {
    const oneOf = "relation";
    return buildError(
      `validator.${parentType}.oneOf`,
      { propertyPath, oneOf },
      errorList,
    );
  }
  return value;
}
function stringValidator45(
  value,
  propertyPath,
  errorList = [],
  parentType = "string",
) {
  if (isNil(value)) {
    return buildError(
      "validator.string.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "string") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (value.length < 1) {
    const min = 1;
    return buildError(
      `validator.${parentType}.min`,
      { propertyPath, min },
      errorList,
    );
  }
  if (value !== "oneToOne" && value !== "oneToMany" && value !== "manyToOne") {
    const oneOf = "oneToOne, oneToMany, manyToOne";
    return buildError(
      `validator.${parentType}.oneOf`,
      { propertyPath, oneOf },
      errorList,
    );
  }
  return value;
}
function referenceValidator46(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return buildError(
      "validator.reference.undefined",
      { propertyPath },
      errorList,
    );
  }
  return codeGenValidators.type(value, propertyPath);
}
function referenceValidator47(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return buildError(
      "validator.reference.undefined",
      { propertyPath },
      errorList,
    );
  }
  return codeGenValidators.type(value, propertyPath);
}
function objectValidator43(
  value,
  propertyPath,
  errorList = [],
  parentType = "object",
) {
  if (isNil(value)) {
    return buildError(
      "validator.object.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  result["type"] = stringValidator44(
    value["type"],
    `${propertyPath}.` + `type`,
    errorList,
  );
  result["docString"] = stringValidator2(
    value["docString"],
    `${propertyPath}.` + `docString`,
    errorList,
  );
  result["isOptional"] = booleanValidator3(
    value["isOptional"],
    `${propertyPath}.` + `isOptional`,
    errorList,
  );
  result["defaultValue"] = anyOfValidator4(
    value["defaultValue"],
    `${propertyPath}.` + `defaultValue`,
    errorList,
  );
  result["uniqueName"] = stringValidator8(
    value["uniqueName"],
    `${propertyPath}.` + `uniqueName`,
    errorList,
  );
  result["group"] = stringValidator8(
    value["group"],
    `${propertyPath}.` + `group`,
    errorList,
  );
  result["name"] = stringValidator8(
    value["name"],
    `${propertyPath}.` + `name`,
    errorList,
  );
  result["relationType"] = stringValidator45(
    value["relationType"],
    `${propertyPath}.` + `relationType`,
    errorList,
  );
  result["left"] = referenceValidator46(
    value["left"],
    `${propertyPath}.` + `left`,
    errorList,
  );
  result["leftKey"] = stringValidator38(
    value["leftKey"],
    `${propertyPath}.` + `leftKey`,
    errorList,
  );
  result["right"] = referenceValidator47(
    value["right"],
    `${propertyPath}.` + `right`,
    errorList,
  );
  result["rightKey"] = stringValidator38(
    value["rightKey"],
    `${propertyPath}.` + `rightKey`,
    errorList,
  );
  result["substituteKey"] = stringValidator38(
    value["substituteKey"],
    `${propertyPath}.` + `substituteKey`,
    errorList,
  );
  return result;
}
function stringValidator49(
  value,
  propertyPath,
  errorList = [],
  parentType = "string",
) {
  if (isNil(value)) {
    return buildError(
      "validator.string.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "string") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (value.length < 1) {
    const min = 1;
    return buildError(
      `validator.${parentType}.min`,
      { propertyPath, min },
      errorList,
    );
  }
  if (value !== "string") {
    const oneOf = "string";
    return buildError(
      `validator.${parentType}.oneOf`,
      { propertyPath, oneOf },
      errorList,
    );
  }
  return value;
}
function arrayValidator50(
  value,
  propertyPath,
  errorList = [],
  parentType = "array",
) {
  if (isNil(value)) {
    return;
  }
  if (!Array.isArray(value)) {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = [];
  for (let i = 0; i < value.length; ++i) {
    result.push(
      stringValidator38(value[i], `${propertyPath}[${i}]`, errorList),
    );
  }
  return result;
}
function numberValidator52(
  value,
  propertyPath,
  errorList = [],
  parentType = "number",
) {
  if (isNil(value)) {
    return 1;
  }
  if (typeof value !== "number" || isNaN(value) || !isFinite(value)) {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (!Number.isInteger(value)) {
    return buildError(
      `validator.${parentType}.integer`,
      { propertyPath },
      errorList,
    );
  }
  return value;
}
function objectValidator51(
  value,
  propertyPath,
  errorList = [],
  parentType = "object",
) {
  if (isNil(value)) {
    return buildError(
      "validator.object.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  result["convert"] = booleanValidator3(
    value["convert"],
    `${propertyPath}.` + `convert`,
    errorList,
  );
  result["trim"] = booleanValidator3(
    value["trim"],
    `${propertyPath}.` + `trim`,
    errorList,
  );
  result["lowerCase"] = booleanValidator3(
    value["lowerCase"],
    `${propertyPath}.` + `lowerCase`,
    errorList,
  );
  result["upperCase"] = booleanValidator3(
    value["upperCase"],
    `${propertyPath}.` + `upperCase`,
    errorList,
  );
  result["min"] = numberValidator52(
    value["min"],
    `${propertyPath}.` + `min`,
    errorList,
  );
  result["max"] = numberValidator16(
    value["max"],
    `${propertyPath}.` + `max`,
    errorList,
  );
  result["pattern"] = stringValidator8(
    value["pattern"],
    `${propertyPath}.` + `pattern`,
    errorList,
  );
  return result;
}
function objectValidator48(
  value,
  propertyPath,
  errorList = [],
  parentType = "object",
) {
  if (isNil(value)) {
    return buildError(
      "validator.object.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  result["type"] = stringValidator49(
    value["type"],
    `${propertyPath}.` + `type`,
    errorList,
  );
  result["docString"] = stringValidator2(
    value["docString"],
    `${propertyPath}.` + `docString`,
    errorList,
  );
  result["isOptional"] = booleanValidator3(
    value["isOptional"],
    `${propertyPath}.` + `isOptional`,
    errorList,
  );
  result["defaultValue"] = anyOfValidator4(
    value["defaultValue"],
    `${propertyPath}.` + `defaultValue`,
    errorList,
  );
  result["uniqueName"] = stringValidator8(
    value["uniqueName"],
    `${propertyPath}.` + `uniqueName`,
    errorList,
  );
  result["group"] = stringValidator8(
    value["group"],
    `${propertyPath}.` + `group`,
    errorList,
  );
  result["name"] = stringValidator8(
    value["name"],
    `${propertyPath}.` + `name`,
    errorList,
  );
  result["oneOf"] = arrayValidator50(
    value["oneOf"],
    `${propertyPath}.` + `oneOf`,
    errorList,
  );
  result["validator"] = objectValidator51(
    value["validator"],
    `${propertyPath}.` + `validator`,
    errorList,
  );
  return result;
}
function stringValidator54(
  value,
  propertyPath,
  errorList = [],
  parentType = "string",
) {
  if (isNil(value)) {
    return buildError(
      "validator.string.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "string") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (value.length < 1) {
    const min = 1;
    return buildError(
      `validator.${parentType}.min`,
      { propertyPath, min },
      errorList,
    );
  }
  if (value !== "uuid") {
    const oneOf = "uuid";
    return buildError(
      `validator.${parentType}.oneOf`,
      { propertyPath, oneOf },
      errorList,
    );
  }
  return value;
}
function objectValidator53(
  value,
  propertyPath,
  errorList = [],
  parentType = "object",
) {
  if (isNil(value)) {
    return buildError(
      "validator.object.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  result["type"] = stringValidator54(
    value["type"],
    `${propertyPath}.` + `type`,
    errorList,
  );
  result["docString"] = stringValidator2(
    value["docString"],
    `${propertyPath}.` + `docString`,
    errorList,
  );
  result["isOptional"] = booleanValidator3(
    value["isOptional"],
    `${propertyPath}.` + `isOptional`,
    errorList,
  );
  result["defaultValue"] = anyOfValidator4(
    value["defaultValue"],
    `${propertyPath}.` + `defaultValue`,
    errorList,
  );
  result["uniqueName"] = stringValidator8(
    value["uniqueName"],
    `${propertyPath}.` + `uniqueName`,
    errorList,
  );
  result["group"] = stringValidator8(
    value["group"],
    `${propertyPath}.` + `group`,
    errorList,
  );
  result["name"] = stringValidator8(
    value["name"],
    `${propertyPath}.` + `name`,
    errorList,
  );
  return result;
}
function stringValidator56(
  value,
  propertyPath,
  errorList = [],
  parentType = "string",
) {
  if (isNil(value)) {
    return buildError(
      "validator.string.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "string") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (value.length < 1) {
    const min = 1;
    return buildError(
      `validator.${parentType}.min`,
      { propertyPath, min },
      errorList,
    );
  }
  if (value !== "route") {
    const oneOf = "route";
    return buildError(
      `validator.${parentType}.oneOf`,
      { propertyPath, oneOf },
      errorList,
    );
  }
  return value;
}
function stringValidator57(
  value,
  propertyPath,
  errorList = [],
  parentType = "string",
) {
  if (isNil(value)) {
    return buildError(
      "validator.string.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "string") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (value.length < 1) {
    const min = 1;
    return buildError(
      `validator.${parentType}.min`,
      { propertyPath, min },
      errorList,
    );
  }
  if (
    value !== "GET" &&
    value !== "POST" &&
    value !== "PUT" &&
    value !== "DELETE" &&
    value !== "HEAD"
  ) {
    const oneOf = "GET, POST, PUT, DELETE, HEAD";
    return buildError(
      `validator.${parentType}.oneOf`,
      { propertyPath, oneOf },
      errorList,
    );
  }
  return value;
}
function arrayValidator58(
  value,
  propertyPath,
  errorList = [],
  parentType = "array",
) {
  if (isNil(value)) {
    return buildError("validator.array.undefined", { propertyPath }, errorList);
  }
  if (!Array.isArray(value)) {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = [];
  for (let i = 0; i < value.length; ++i) {
    result.push(
      stringValidator38(value[i], `${propertyPath}[${i}]`, errorList),
    );
  }
  return result;
}
function referenceValidator59(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return;
  }
  return codeGenValidators.type(value, propertyPath);
}
function referenceValidator60(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return;
  }
  return codeGenValidators.type(value, propertyPath);
}
function referenceValidator61(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return;
  }
  return codeGenValidators.type(value, propertyPath);
}
function referenceValidator62(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return;
  }
  return codeGenValidators.type(value, propertyPath);
}
function referenceValidator63(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return;
  }
  return codeGenValidators.type(value, propertyPath);
}
function objectValidator55(
  value,
  propertyPath,
  errorList = [],
  parentType = "object",
) {
  if (isNil(value)) {
    return buildError(
      "validator.object.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  result["type"] = stringValidator56(
    value["type"],
    `${propertyPath}.` + `type`,
    errorList,
  );
  result["docString"] = stringValidator2(
    value["docString"],
    `${propertyPath}.` + `docString`,
    errorList,
  );
  result["isOptional"] = booleanValidator3(
    value["isOptional"],
    `${propertyPath}.` + `isOptional`,
    errorList,
  );
  result["defaultValue"] = anyOfValidator4(
    value["defaultValue"],
    `${propertyPath}.` + `defaultValue`,
    errorList,
  );
  result["uniqueName"] = stringValidator8(
    value["uniqueName"],
    `${propertyPath}.` + `uniqueName`,
    errorList,
  );
  result["group"] = stringValidator8(
    value["group"],
    `${propertyPath}.` + `group`,
    errorList,
  );
  result["name"] = stringValidator8(
    value["name"],
    `${propertyPath}.` + `name`,
    errorList,
  );
  result["method"] = stringValidator57(
    value["method"],
    `${propertyPath}.` + `method`,
    errorList,
  );
  result["path"] = stringValidator38(
    value["path"],
    `${propertyPath}.` + `path`,
    errorList,
  );
  result["tags"] = arrayValidator58(
    value["tags"],
    `${propertyPath}.` + `tags`,
    errorList,
  );
  result["query"] = referenceValidator59(
    value["query"],
    `${propertyPath}.` + `query`,
    errorList,
  );
  result["params"] = referenceValidator60(
    value["params"],
    `${propertyPath}.` + `params`,
    errorList,
  );
  result["body"] = referenceValidator61(
    value["body"],
    `${propertyPath}.` + `body`,
    errorList,
  );
  result["files"] = referenceValidator62(
    value["files"],
    `${propertyPath}.` + `files`,
    errorList,
  );
  result["response"] = referenceValidator63(
    value["response"],
    `${propertyPath}.` + `response`,
    errorList,
  );
  return result;
}
function referenceValidator65(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return buildError(
      "validator.reference.undefined",
      { propertyPath },
      errorList,
    );
  }
  return codeGenValidators.anyType(value, propertyPath);
}
function referenceValidator66(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return buildError(
      "validator.reference.undefined",
      { propertyPath },
      errorList,
    );
  }
  return codeGenValidators.anyOfType(value, propertyPath);
}
function referenceValidator67(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return buildError(
      "validator.reference.undefined",
      { propertyPath },
      errorList,
    );
  }
  return codeGenValidators.arrayType(value, propertyPath);
}
function referenceValidator68(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return buildError(
      "validator.reference.undefined",
      { propertyPath },
      errorList,
    );
  }
  return codeGenValidators.booleanType(value, propertyPath);
}
function referenceValidator69(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return buildError(
      "validator.reference.undefined",
      { propertyPath },
      errorList,
    );
  }
  return codeGenValidators.dateType(value, propertyPath);
}
function referenceValidator70(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return buildError(
      "validator.reference.undefined",
      { propertyPath },
      errorList,
    );
  }
  return codeGenValidators.fileType(value, propertyPath);
}
function referenceValidator71(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return buildError(
      "validator.reference.undefined",
      { propertyPath },
      errorList,
    );
  }
  return codeGenValidators.genericType(value, propertyPath);
}
function referenceValidator72(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return buildError(
      "validator.reference.undefined",
      { propertyPath },
      errorList,
    );
  }
  return codeGenValidators.numberType(value, propertyPath);
}
function referenceValidator73(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return buildError(
      "validator.reference.undefined",
      { propertyPath },
      errorList,
    );
  }
  return codeGenValidators.objectType(value, propertyPath);
}
function referenceValidator74(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return buildError(
      "validator.reference.undefined",
      { propertyPath },
      errorList,
    );
  }
  return codeGenValidators.referenceType(value, propertyPath);
}
function referenceValidator75(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return buildError(
      "validator.reference.undefined",
      { propertyPath },
      errorList,
    );
  }
  return codeGenValidators.relationType(value, propertyPath);
}
function referenceValidator76(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return buildError(
      "validator.reference.undefined",
      { propertyPath },
      errorList,
    );
  }
  return codeGenValidators.stringType(value, propertyPath);
}
function referenceValidator77(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return buildError(
      "validator.reference.undefined",
      { propertyPath },
      errorList,
    );
  }
  return codeGenValidators.uuidType(value, propertyPath);
}
function referenceValidator78(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return buildError(
      "validator.reference.undefined",
      { propertyPath },
      errorList,
    );
  }
  return codeGenValidators.routeType(value, propertyPath);
}
function anyOfValidator64(
  value,
  propertyPath,
  errorList = [],
  parentType = "anyOf",
) {
  if (isNil(value)) {
    return buildError("validator.anyOf.undefined", { propertyPath }, errorList);
  }
  const errors = [];
  try {
    return referenceValidator65(value, propertyPath, errorList);
  } catch (e) {
    errors.push({
      name: e.name,
      message: e.message,
      key: e.key,
      info: e.info,
    });
  }
  try {
    return referenceValidator66(value, propertyPath, errorList);
  } catch (e) {
    errors.push({
      name: e.name,
      message: e.message,
      key: e.key,
      info: e.info,
    });
  }
  try {
    return referenceValidator67(value, propertyPath, errorList);
  } catch (e) {
    errors.push({
      name: e.name,
      message: e.message,
      key: e.key,
      info: e.info,
    });
  }
  try {
    return referenceValidator68(value, propertyPath, errorList);
  } catch (e) {
    errors.push({
      name: e.name,
      message: e.message,
      key: e.key,
      info: e.info,
    });
  }
  try {
    return referenceValidator69(value, propertyPath, errorList);
  } catch (e) {
    errors.push({
      name: e.name,
      message: e.message,
      key: e.key,
      info: e.info,
    });
  }
  try {
    return referenceValidator70(value, propertyPath, errorList);
  } catch (e) {
    errors.push({
      name: e.name,
      message: e.message,
      key: e.key,
      info: e.info,
    });
  }
  try {
    return referenceValidator71(value, propertyPath, errorList);
  } catch (e) {
    errors.push({
      name: e.name,
      message: e.message,
      key: e.key,
      info: e.info,
    });
  }
  try {
    return referenceValidator72(value, propertyPath, errorList);
  } catch (e) {
    errors.push({
      name: e.name,
      message: e.message,
      key: e.key,
      info: e.info,
    });
  }
  try {
    return referenceValidator73(value, propertyPath, errorList);
  } catch (e) {
    errors.push({
      name: e.name,
      message: e.message,
      key: e.key,
      info: e.info,
    });
  }
  try {
    return referenceValidator74(value, propertyPath, errorList);
  } catch (e) {
    errors.push({
      name: e.name,
      message: e.message,
      key: e.key,
      info: e.info,
    });
  }
  try {
    return referenceValidator75(value, propertyPath, errorList);
  } catch (e) {
    errors.push({
      name: e.name,
      message: e.message,
      key: e.key,
      info: e.info,
    });
  }
  try {
    return referenceValidator76(value, propertyPath, errorList);
  } catch (e) {
    errors.push({
      name: e.name,
      message: e.message,
      key: e.key,
      info: e.info,
    });
  }
  try {
    return referenceValidator77(value, propertyPath, errorList);
  } catch (e) {
    errors.push({
      name: e.name,
      message: e.message,
      key: e.key,
      info: e.info,
    });
  }
  try {
    return referenceValidator78(value, propertyPath, errorList);
  } catch (e) {
    errors.push({
      name: e.name,
      message: e.message,
      key: e.key,
      info: e.info,
    });
  }
  return buildError(
    `validator.${parentType}.type`,
    { propertyPath, errors },
    errorList,
  );
}
function referenceValidator81(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return buildError(
      "validator.reference.undefined",
      { propertyPath },
      errorList,
    );
  }
  return codeGenValidators.type(value, propertyPath);
}
function genericValidator80(
  value,
  propertyPath,
  errorList = [],
  parentType = "generic",
) {
  if (isNil(value)) {
    return buildError(
      "validator.generic.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  for (const key of Object.keys(value)) {
    const validatedKey = stringValidator38(
      key,
      `${propertyPath}.$key[${key}]`,
      errorList,
    );
    const validatedValue = referenceValidator81(
      value[key],
      `${propertyPath}.$value[${key}]`,
      errorList,
    );
    result[validatedKey] = validatedValue;
  }
  return result;
}
function genericValidator79(
  value,
  propertyPath,
  errorList = [],
  parentType = "generic",
) {
  if (isNil(value)) {
    return buildError(
      "validator.generic.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  for (const key of Object.keys(value)) {
    const validatedKey = stringValidator38(
      key,
      `${propertyPath}.$key[${key}]`,
      errorList,
    );
    const validatedValue = genericValidator80(
      value[key],
      `${propertyPath}.$value[${key}]`,
      errorList,
    );
    result[validatedKey] = validatedValue;
  }
  return result;
}
function stringValidator83(
  value,
  propertyPath,
  errorList = [],
  parentType = "string",
) {
  if (isNil(value)) {
    return buildError(
      "validator.string.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "string") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (value.length < 1) {
    const min = 1;
    return buildError(
      `validator.${parentType}.min`,
      { propertyPath, min },
      errorList,
    );
  }
  if (value !== "init" && value !== "collect" && value !== "finish") {
    const oneOf = "init, collect, finish";
    return buildError(
      `validator.${parentType}.oneOf`,
      { propertyPath, oneOf },
      errorList,
    );
  }
  return value;
}
function referenceValidator84(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return buildError(
      "validator.reference.undefined",
      { propertyPath },
      errorList,
    );
  }
  return codeGenValidators.templateImports(value, propertyPath);
}
function objectValidator82(
  value,
  propertyPath,
  errorList = [],
  parentType = "object",
) {
  if (isNil(value)) {
    return buildError(
      "validator.object.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  result["phase"] = stringValidator83(
    value["phase"],
    `${propertyPath}.` + `phase`,
    errorList,
  );
  result["imports"] = referenceValidator84(
    value["imports"],
    `${propertyPath}.` + `imports`,
    errorList,
  );
  return result;
}
function anyValidator87(
  value,
  propertyPath,
  errorList = [],
  parentType = "any",
) {
  if (isNil(value)) {
    return buildError("validator.any.undefined", { propertyPath }, errorList);
  }
  if (!(value instanceof Set)) {
    const instanceOf = "Set";
    return buildError(
      `validator.${parentType}.instance`,
      { propertyPath, instanceOf },
      errorList,
    );
  }
  return value;
}
function genericValidator86(
  value,
  propertyPath,
  errorList = [],
  parentType = "generic",
) {
  if (isNil(value)) {
    return buildError(
      "validator.generic.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  for (const key of Object.keys(value)) {
    const validatedKey = stringValidator38(
      key,
      `${propertyPath}.$key[${key}]`,
      errorList,
    );
    const validatedValue = anyValidator87(
      value[key],
      `${propertyPath}.$value[${key}]`,
      errorList,
    );
    result[validatedKey] = validatedValue;
  }
  return result;
}
function anyValidator88(
  value,
  propertyPath,
  errorList = [],
  parentType = "any",
) {
  if (isNil(value)) {
    return buildError("validator.any.undefined", { propertyPath }, errorList);
  }
  if (!(value instanceof Map)) {
    const instanceOf = "Map";
    return buildError(
      `validator.${parentType}.instance`,
      { propertyPath, instanceOf },
      errorList,
    );
  }
  return value;
}
function objectValidator85(
  value,
  propertyPath,
  errorList = [],
  parentType = "object",
) {
  if (isNil(value)) {
    return;
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  result["destructureImport"] = genericValidator86(
    value["destructureImport"],
    `${propertyPath}.` + `destructureImport`,
    errorList,
  );
  result["starImport"] = anyValidator88(
    value["starImport"],
    `${propertyPath}.` + `starImport`,
    errorList,
  );
  result["commonjsImport"] = anyValidator88(
    value["commonjsImport"],
    `${propertyPath}.` + `commonjsImport`,
    errorList,
  );
  return result;
}
