// Generated by @lbu/code-gen
/* eslint-disable no-unused-vars */

export const validatorHooks = {};

const isNil = (value) => value === undefined || value === null;

/**
 * This function should not throw as the corresponding validator will do that
 * @callback ValidationErrorFn
 * @param {string} key
 * @param {Object} info
 */

/**
 * @type ValidationErrorFn
 */
let _errorBuildFn = (key, info) => {
  const err = new Error(`ValidationError: ${key}`);
  err.key = key;
  err.info = info;
  return err;
};

/**
 * Set a different error function, for example AppError.validationError
 * @param {ValidationErrorFn} fn
 */
export function validatorSetError(fn) {
  _errorBuildFn = fn;
}

function buildError(key, info, list) {
  list.push(_errorBuildFn(key, info));
  return undefined;
}

export const codeGenValidators = {
  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { { errors: ({ key: string, info: object}[]|undefined), data: CodeGenAnyOfType|undefined } }
   */
  anyOfType: (value, propertyPath = "$") => {
    let result = value;
    if ("preValidateCodeGenAnyOfType" in validatorHooks) {
      result = validatorHooks["preValidateCodeGenAnyOfType"](result);
    }
    const errorList = [];
    result = objectValidator0(result, propertyPath, errorList, "object");
    if (errorList.length > 0) {
      return { errors: errorList };
    }
    if ("postValidateCodeGenAnyOfType" in validatorHooks) {
      return validatorHooks["postValidateCodeGenAnyOfType"]({ data: result });
    }
    return { data: result };
  },

  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { { errors: ({ key: string, info: object}[]|undefined), data: CodeGenAnyType|undefined } }
   */
  anyType: (value, propertyPath = "$") => {
    let result = value;
    if ("preValidateCodeGenAnyType" in validatorHooks) {
      result = validatorHooks["preValidateCodeGenAnyType"](result);
    }
    const errorList = [];
    result = objectValidator12(result, propertyPath, errorList, "object");
    if (errorList.length > 0) {
      return { errors: errorList };
    }
    if ("postValidateCodeGenAnyType" in validatorHooks) {
      return validatorHooks["postValidateCodeGenAnyType"]({ data: result });
    }
    return { data: result };
  },

  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { { errors: ({ key: string, info: object}[]|undefined), data: CodeGenArrayType|undefined } }
   */
  arrayType: (value, propertyPath = "$") => {
    let result = value;
    if ("preValidateCodeGenArrayType" in validatorHooks) {
      result = validatorHooks["preValidateCodeGenArrayType"](result);
    }
    const errorList = [];
    result = objectValidator14(result, propertyPath, errorList, "object");
    if (errorList.length > 0) {
      return { errors: errorList };
    }
    if ("postValidateCodeGenArrayType" in validatorHooks) {
      return validatorHooks["postValidateCodeGenArrayType"]({ data: result });
    }
    return { data: result };
  },

  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { { errors: ({ key: string, info: object}[]|undefined), data: CodeGenBooleanType|undefined } }
   */
  booleanType: (value, propertyPath = "$") => {
    let result = value;
    if ("preValidateCodeGenBooleanType" in validatorHooks) {
      result = validatorHooks["preValidateCodeGenBooleanType"](result);
    }
    const errorList = [];
    result = objectValidator19(result, propertyPath, errorList, "object");
    if (errorList.length > 0) {
      return { errors: errorList };
    }
    if ("postValidateCodeGenBooleanType" in validatorHooks) {
      return validatorHooks["postValidateCodeGenBooleanType"]({ data: result });
    }
    return { data: result };
  },

  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { { errors: ({ key: string, info: object}[]|undefined), data: CodeGenDateType|undefined } }
   */
  dateType: (value, propertyPath = "$") => {
    let result = value;
    if ("preValidateCodeGenDateType" in validatorHooks) {
      result = validatorHooks["preValidateCodeGenDateType"](result);
    }
    const errorList = [];
    result = objectValidator23(result, propertyPath, errorList, "object");
    if (errorList.length > 0) {
      return { errors: errorList };
    }
    if ("postValidateCodeGenDateType" in validatorHooks) {
      return validatorHooks["postValidateCodeGenDateType"]({ data: result });
    }
    return { data: result };
  },

  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { { errors: ({ key: string, info: object}[]|undefined), data: CodeGenFileType|undefined } }
   */
  fileType: (value, propertyPath = "$") => {
    let result = value;
    if ("preValidateCodeGenFileType" in validatorHooks) {
      result = validatorHooks["preValidateCodeGenFileType"](result);
    }
    const errorList = [];
    result = objectValidator25(result, propertyPath, errorList, "object");
    if (errorList.length > 0) {
      return { errors: errorList };
    }
    if ("postValidateCodeGenFileType" in validatorHooks) {
      return validatorHooks["postValidateCodeGenFileType"]({ data: result });
    }
    return { data: result };
  },

  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { { errors: ({ key: string, info: object}[]|undefined), data: CodeGenGenericType|undefined } }
   */
  genericType: (value, propertyPath = "$") => {
    let result = value;
    if ("preValidateCodeGenGenericType" in validatorHooks) {
      result = validatorHooks["preValidateCodeGenGenericType"](result);
    }
    const errorList = [];
    result = objectValidator27(result, propertyPath, errorList, "object");
    if (errorList.length > 0) {
      return { errors: errorList };
    }
    if ("postValidateCodeGenGenericType" in validatorHooks) {
      return validatorHooks["postValidateCodeGenGenericType"]({ data: result });
    }
    return { data: result };
  },

  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { { errors: ({ key: string, info: object}[]|undefined), data: CodeGenNumberType|undefined } }
   */
  numberType: (value, propertyPath = "$") => {
    let result = value;
    if ("preValidateCodeGenNumberType" in validatorHooks) {
      result = validatorHooks["preValidateCodeGenNumberType"](result);
    }
    const errorList = [];
    result = objectValidator31(result, propertyPath, errorList, "object");
    if (errorList.length > 0) {
      return { errors: errorList };
    }
    if ("postValidateCodeGenNumberType" in validatorHooks) {
      return validatorHooks["postValidateCodeGenNumberType"]({ data: result });
    }
    return { data: result };
  },

  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { { errors: ({ key: string, info: object}[]|undefined), data: CodeGenObjectType|undefined } }
   */
  objectType: (value, propertyPath = "$") => {
    let result = value;
    if ("preValidateCodeGenObjectType" in validatorHooks) {
      result = validatorHooks["preValidateCodeGenObjectType"](result);
    }
    const errorList = [];
    result = objectValidator35(result, propertyPath, errorList, "object");
    if (errorList.length > 0) {
      return { errors: errorList };
    }
    if ("postValidateCodeGenObjectType" in validatorHooks) {
      return validatorHooks["postValidateCodeGenObjectType"]({ data: result });
    }
    return { data: result };
  },

  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { { errors: ({ key: string, info: object}[]|undefined), data: CodeGenReferenceType|undefined } }
   */
  referenceType: (value, propertyPath = "$") => {
    let result = value;
    if ("preValidateCodeGenReferenceType" in validatorHooks) {
      result = validatorHooks["preValidateCodeGenReferenceType"](result);
    }
    const errorList = [];
    result = objectValidator43(result, propertyPath, errorList, "object");
    if (errorList.length > 0) {
      return { errors: errorList };
    }
    if ("postValidateCodeGenReferenceType" in validatorHooks) {
      return validatorHooks["postValidateCodeGenReferenceType"]({
        data: result,
      });
    }
    return { data: result };
  },

  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { { errors: ({ key: string, info: object}[]|undefined), data: CodeGenRelationType|undefined } }
   */
  relationType: (value, propertyPath = "$") => {
    let result = value;
    if ("preValidateCodeGenRelationType" in validatorHooks) {
      result = validatorHooks["preValidateCodeGenRelationType"](result);
    }
    const errorList = [];
    result = objectValidator46(result, propertyPath, errorList, "object");
    if (errorList.length > 0) {
      return { errors: errorList };
    }
    if ("postValidateCodeGenRelationType" in validatorHooks) {
      return validatorHooks["postValidateCodeGenRelationType"]({
        data: result,
      });
    }
    return { data: result };
  },

  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { { errors: ({ key: string, info: object}[]|undefined), data: CodeGenRouteType|undefined } }
   */
  routeType: (value, propertyPath = "$") => {
    let result = value;
    if ("preValidateCodeGenRouteType" in validatorHooks) {
      result = validatorHooks["preValidateCodeGenRouteType"](result);
    }
    const errorList = [];
    result = objectValidator51(result, propertyPath, errorList, "object");
    if (errorList.length > 0) {
      return { errors: errorList };
    }
    if ("postValidateCodeGenRouteType" in validatorHooks) {
      return validatorHooks["postValidateCodeGenRouteType"]({ data: result });
    }
    return { data: result };
  },

  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { { errors: ({ key: string, info: object}[]|undefined), data: CodeGenStringType|undefined } }
   */
  stringType: (value, propertyPath = "$") => {
    let result = value;
    if ("preValidateCodeGenStringType" in validatorHooks) {
      result = validatorHooks["preValidateCodeGenStringType"](result);
    }
    const errorList = [];
    result = objectValidator60(result, propertyPath, errorList, "object");
    if (errorList.length > 0) {
      return { errors: errorList };
    }
    if ("postValidateCodeGenStringType" in validatorHooks) {
      return validatorHooks["postValidateCodeGenStringType"]({ data: result });
    }
    return { data: result };
  },

  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { { errors: ({ key: string, info: object}[]|undefined), data: CodeGenStructure|undefined } }
   */
  structure: (value, propertyPath = "$") => {
    let result = value;
    if ("preValidateCodeGenStructure" in validatorHooks) {
      result = validatorHooks["preValidateCodeGenStructure"](result);
    }
    const errorList = [];
    result = genericValidator65(result, propertyPath, errorList, "generic");
    if (errorList.length > 0) {
      return { errors: errorList };
    }
    if ("postValidateCodeGenStructure" in validatorHooks) {
      return validatorHooks["postValidateCodeGenStructure"]({ data: result });
    }
    return { data: result };
  },

  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { { errors: ({ key: string, info: object}[]|undefined), data: CodeGenType|undefined } }
   */
  type: (value, propertyPath = "$") => {
    let result = value;
    if ("preValidateCodeGenType" in validatorHooks) {
      result = validatorHooks["preValidateCodeGenType"](result);
    }
    const errorList = [];
    result = anyOfValidator68(result, propertyPath, errorList, "anyOf");
    if (errorList.length > 0) {
      return { errors: errorList };
    }
    if ("postValidateCodeGenType" in validatorHooks) {
      return validatorHooks["postValidateCodeGenType"]({ data: result });
    }
    return { data: result };
  },

  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { { errors: ({ key: string, info: object}[]|undefined), data: CodeGenUuidType|undefined } }
   */
  uuidType: (value, propertyPath = "$") => {
    let result = value;
    if ("preValidateCodeGenUuidType" in validatorHooks) {
      result = validatorHooks["preValidateCodeGenUuidType"](result);
    }
    const errorList = [];
    result = objectValidator83(result, propertyPath, errorList, "object");
    if (errorList.length > 0) {
      return { errors: errorList };
    }
    if ("postValidateCodeGenUuidType" in validatorHooks) {
      return validatorHooks["postValidateCodeGenUuidType"]({ data: result });
    }
    return { data: result };
  },
};

function stringValidator1(
  value,
  propertyPath,
  errorList = [],
  parentType = "string",
) {
  if (isNil(value)) {
    return buildError(
      "validator.string.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "string") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (value.length < 1) {
    const min = 1;
    return buildError(
      `validator.${parentType}.min`,
      { propertyPath, min },
      errorList,
    );
  }
  if (value !== "anyOf") {
    const oneOf = "anyOf";
    return buildError(
      `validator.${parentType}.oneOf`,
      { propertyPath, oneOf },
      errorList,
    );
  }
  return value;
}
function stringValidator2(
  value,
  propertyPath,
  errorList = [],
  parentType = "string",
) {
  if (isNil(value)) {
    return;
  }
  if (typeof value !== "string") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (value.length < 0) {
    const min = 0;
    return buildError(
      `validator.${parentType}.min`,
      { propertyPath, min },
      errorList,
    );
  }
  return value;
}
function booleanValidator3(
  value,
  propertyPath,
  errorList = [],
  parentType = "boolean",
) {
  if (isNil(value)) {
    return false;
  }
  if (typeof value !== "boolean") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  return value;
}
function stringValidator5(
  value,
  propertyPath,
  errorList = [],
  parentType = "string",
) {
  if (isNil(value)) {
    return buildError(
      "validator.string.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "string") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (value.length < 0) {
    const min = 0;
    return buildError(
      `validator.${parentType}.min`,
      { propertyPath, min },
      errorList,
    );
  }
  return value;
}
function booleanValidator6(
  value,
  propertyPath,
  errorList = [],
  parentType = "boolean",
) {
  if (isNil(value)) {
    return buildError(
      "validator.boolean.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "boolean") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  return value;
}
function numberValidator7(
  value,
  propertyPath,
  errorList = [],
  parentType = "number",
) {
  if (isNil(value)) {
    return buildError(
      "validator.number.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "number" || isNaN(value) || !isFinite(value)) {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (!Number.isInteger(value)) {
    return buildError(
      `validator.${parentType}.integer`,
      { propertyPath },
      errorList,
    );
  }
  return value;
}
function anyOfValidator4(
  value,
  propertyPath,
  errorList = [],
  parentType = "anyOf",
) {
  if (isNil(value)) {
    return;
  }
  let errCount = 0;
  const errors = [];
  let result = undefined;
  result = stringValidator5(value, propertyPath, errors);
  if (errors.length === errCount) {
    return result;
  }
  errCount = errors.length;
  result = booleanValidator6(value, propertyPath, errors);
  if (errors.length === errCount) {
    return result;
  }
  errCount = errors.length;
  result = numberValidator7(value, propertyPath, errors);
  if (errors.length === errCount) {
    return result;
  }
  errCount = errors.length;
  return buildError(
    `validator.${parentType}.type`,
    { propertyPath, errors },
    errorList,
  );
}
function stringValidator8(
  value,
  propertyPath,
  errorList = [],
  parentType = "string",
) {
  if (isNil(value)) {
    return;
  }
  if (typeof value !== "string") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (value.length < 1) {
    const min = 1;
    return buildError(
      `validator.${parentType}.min`,
      { propertyPath, min },
      errorList,
    );
  }
  return value;
}
function objectValidator9(
  value,
  propertyPath,
  errorList = [],
  parentType = "object",
) {
  if (isNil(value)) {
    return;
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  const keySet = new Set(Object.keys(value));
  result["primary"] = booleanValidator3(
    value["primary"],
    `${propertyPath}.` + `primary`,
    errorList,
  );
  keySet.delete("primary");
  result["searchable"] = booleanValidator3(
    value["searchable"],
    `${propertyPath}.` + `searchable`,
    errorList,
  );
  keySet.delete("searchable");
  if (keySet.size !== 0) {
    let extraKeys = "";
    for (const k of keySet.keys()) {
      extraKeys += `${k},`;
    }
    return buildError(
      `validator.${parentType}.strict`,
      { propertyPath, extraKeys },
      errorList,
    );
  }
  return result;
}
function referenceValidator11(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return buildError(
      "validator.reference.undefined",
      { propertyPath },
      errorList,
    );
  }
  const { errors, data } = codeGenValidators.type(value, propertyPath);
  if (errors !== undefined) {
    errorList.push(...errors);
  }
  return data;
}
function arrayValidator10(
  value,
  propertyPath,
  errorList = [],
  parentType = "array",
) {
  if (isNil(value)) {
    return buildError("validator.array.undefined", { propertyPath }, errorList);
  }
  if (!Array.isArray(value)) {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = [];
  for (let i = 0; i < value.length; ++i) {
    result.push(
      referenceValidator11(value[i], `${propertyPath}[${i}]`, errorList),
    );
  }
  return result;
}
function objectValidator0(
  value,
  propertyPath,
  errorList = [],
  parentType = "object",
) {
  if (isNil(value)) {
    return buildError(
      "validator.object.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  const keySet = new Set(Object.keys(value));
  result["type"] = stringValidator1(
    value["type"],
    `${propertyPath}.` + `type`,
    errorList,
  );
  keySet.delete("type");
  result["docString"] = stringValidator2(
    value["docString"],
    `${propertyPath}.` + `docString`,
    errorList,
  );
  keySet.delete("docString");
  result["isOptional"] = booleanValidator3(
    value["isOptional"],
    `${propertyPath}.` + `isOptional`,
    errorList,
  );
  keySet.delete("isOptional");
  result["defaultValue"] = anyOfValidator4(
    value["defaultValue"],
    `${propertyPath}.` + `defaultValue`,
    errorList,
  );
  keySet.delete("defaultValue");
  result["uniqueName"] = stringValidator8(
    value["uniqueName"],
    `${propertyPath}.` + `uniqueName`,
    errorList,
  );
  keySet.delete("uniqueName");
  result["group"] = stringValidator8(
    value["group"],
    `${propertyPath}.` + `group`,
    errorList,
  );
  keySet.delete("group");
  result["name"] = stringValidator8(
    value["name"],
    `${propertyPath}.` + `name`,
    errorList,
  );
  keySet.delete("name");
  result["sql"] = objectValidator9(
    value["sql"],
    `${propertyPath}.` + `sql`,
    errorList,
  );
  keySet.delete("sql");
  result["values"] = arrayValidator10(
    value["values"],
    `${propertyPath}.` + `values`,
    errorList,
  );
  keySet.delete("values");
  if (keySet.size !== 0) {
    let extraKeys = "";
    for (const k of keySet.keys()) {
      extraKeys += `${k},`;
    }
    return buildError(
      `validator.${parentType}.strict`,
      { propertyPath, extraKeys },
      errorList,
    );
  }
  return result;
}
function stringValidator13(
  value,
  propertyPath,
  errorList = [],
  parentType = "string",
) {
  if (isNil(value)) {
    return buildError(
      "validator.string.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "string") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (value.length < 1) {
    const min = 1;
    return buildError(
      `validator.${parentType}.min`,
      { propertyPath, min },
      errorList,
    );
  }
  if (value !== "any") {
    const oneOf = "any";
    return buildError(
      `validator.${parentType}.oneOf`,
      { propertyPath, oneOf },
      errorList,
    );
  }
  return value;
}
function objectValidator12(
  value,
  propertyPath,
  errorList = [],
  parentType = "object",
) {
  if (isNil(value)) {
    return buildError(
      "validator.object.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  const keySet = new Set(Object.keys(value));
  result["type"] = stringValidator13(
    value["type"],
    `${propertyPath}.` + `type`,
    errorList,
  );
  keySet.delete("type");
  result["docString"] = stringValidator2(
    value["docString"],
    `${propertyPath}.` + `docString`,
    errorList,
  );
  keySet.delete("docString");
  result["isOptional"] = booleanValidator3(
    value["isOptional"],
    `${propertyPath}.` + `isOptional`,
    errorList,
  );
  keySet.delete("isOptional");
  result["defaultValue"] = anyOfValidator4(
    value["defaultValue"],
    `${propertyPath}.` + `defaultValue`,
    errorList,
  );
  keySet.delete("defaultValue");
  result["uniqueName"] = stringValidator8(
    value["uniqueName"],
    `${propertyPath}.` + `uniqueName`,
    errorList,
  );
  keySet.delete("uniqueName");
  result["group"] = stringValidator8(
    value["group"],
    `${propertyPath}.` + `group`,
    errorList,
  );
  keySet.delete("group");
  result["name"] = stringValidator8(
    value["name"],
    `${propertyPath}.` + `name`,
    errorList,
  );
  keySet.delete("name");
  result["sql"] = objectValidator9(
    value["sql"],
    `${propertyPath}.` + `sql`,
    errorList,
  );
  keySet.delete("sql");
  result["typeOf"] = stringValidator8(
    value["typeOf"],
    `${propertyPath}.` + `typeOf`,
    errorList,
  );
  keySet.delete("typeOf");
  result["instanceOf"] = stringValidator8(
    value["instanceOf"],
    `${propertyPath}.` + `instanceOf`,
    errorList,
  );
  keySet.delete("instanceOf");
  if (keySet.size !== 0) {
    let extraKeys = "";
    for (const k of keySet.keys()) {
      extraKeys += `${k},`;
    }
    return buildError(
      `validator.${parentType}.strict`,
      { propertyPath, extraKeys },
      errorList,
    );
  }
  return result;
}
function stringValidator15(
  value,
  propertyPath,
  errorList = [],
  parentType = "string",
) {
  if (isNil(value)) {
    return buildError(
      "validator.string.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "string") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (value.length < 1) {
    const min = 1;
    return buildError(
      `validator.${parentType}.min`,
      { propertyPath, min },
      errorList,
    );
  }
  if (value !== "array") {
    const oneOf = "array";
    return buildError(
      `validator.${parentType}.oneOf`,
      { propertyPath, oneOf },
      errorList,
    );
  }
  return value;
}
function numberValidator17(
  value,
  propertyPath,
  errorList = [],
  parentType = "number",
) {
  if (isNil(value)) {
    return;
  }
  if (typeof value !== "number" || isNaN(value) || !isFinite(value)) {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (!Number.isInteger(value)) {
    return buildError(
      `validator.${parentType}.integer`,
      { propertyPath },
      errorList,
    );
  }
  return value;
}
function objectValidator16(
  value,
  propertyPath,
  errorList = [],
  parentType = "object",
) {
  if (isNil(value)) {
    return buildError(
      "validator.object.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  const keySet = new Set(Object.keys(value));
  result["convert"] = booleanValidator3(
    value["convert"],
    `${propertyPath}.` + `convert`,
    errorList,
  );
  keySet.delete("convert");
  result["min"] = numberValidator17(
    value["min"],
    `${propertyPath}.` + `min`,
    errorList,
  );
  keySet.delete("min");
  result["max"] = numberValidator17(
    value["max"],
    `${propertyPath}.` + `max`,
    errorList,
  );
  keySet.delete("max");
  if (keySet.size !== 0) {
    let extraKeys = "";
    for (const k of keySet.keys()) {
      extraKeys += `${k},`;
    }
    return buildError(
      `validator.${parentType}.strict`,
      { propertyPath, extraKeys },
      errorList,
    );
  }
  return result;
}
function referenceValidator18(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return buildError(
      "validator.reference.undefined",
      { propertyPath },
      errorList,
    );
  }
  const { errors, data } = codeGenValidators.type(value, propertyPath);
  if (errors !== undefined) {
    errorList.push(...errors);
  }
  return data;
}
function objectValidator14(
  value,
  propertyPath,
  errorList = [],
  parentType = "object",
) {
  if (isNil(value)) {
    return buildError(
      "validator.object.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  const keySet = new Set(Object.keys(value));
  result["type"] = stringValidator15(
    value["type"],
    `${propertyPath}.` + `type`,
    errorList,
  );
  keySet.delete("type");
  result["docString"] = stringValidator2(
    value["docString"],
    `${propertyPath}.` + `docString`,
    errorList,
  );
  keySet.delete("docString");
  result["isOptional"] = booleanValidator3(
    value["isOptional"],
    `${propertyPath}.` + `isOptional`,
    errorList,
  );
  keySet.delete("isOptional");
  result["defaultValue"] = anyOfValidator4(
    value["defaultValue"],
    `${propertyPath}.` + `defaultValue`,
    errorList,
  );
  keySet.delete("defaultValue");
  result["uniqueName"] = stringValidator8(
    value["uniqueName"],
    `${propertyPath}.` + `uniqueName`,
    errorList,
  );
  keySet.delete("uniqueName");
  result["group"] = stringValidator8(
    value["group"],
    `${propertyPath}.` + `group`,
    errorList,
  );
  keySet.delete("group");
  result["name"] = stringValidator8(
    value["name"],
    `${propertyPath}.` + `name`,
    errorList,
  );
  keySet.delete("name");
  result["sql"] = objectValidator9(
    value["sql"],
    `${propertyPath}.` + `sql`,
    errorList,
  );
  keySet.delete("sql");
  result["validator"] = objectValidator16(
    value["validator"],
    `${propertyPath}.` + `validator`,
    errorList,
  );
  keySet.delete("validator");
  result["values"] = referenceValidator18(
    value["values"],
    `${propertyPath}.` + `values`,
    errorList,
  );
  keySet.delete("values");
  if (keySet.size !== 0) {
    let extraKeys = "";
    for (const k of keySet.keys()) {
      extraKeys += `${k},`;
    }
    return buildError(
      `validator.${parentType}.strict`,
      { propertyPath, extraKeys },
      errorList,
    );
  }
  return result;
}
function stringValidator20(
  value,
  propertyPath,
  errorList = [],
  parentType = "string",
) {
  if (isNil(value)) {
    return buildError(
      "validator.string.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "string") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (value.length < 1) {
    const min = 1;
    return buildError(
      `validator.${parentType}.min`,
      { propertyPath, min },
      errorList,
    );
  }
  if (value !== "boolean") {
    const oneOf = "boolean";
    return buildError(
      `validator.${parentType}.oneOf`,
      { propertyPath, oneOf },
      errorList,
    );
  }
  return value;
}
function booleanValidator21(
  value,
  propertyPath,
  errorList = [],
  parentType = "boolean",
) {
  if (isNil(value)) {
    return;
  }
  if (typeof value !== "boolean") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  return value;
}
function objectValidator22(
  value,
  propertyPath,
  errorList = [],
  parentType = "object",
) {
  if (isNil(value)) {
    return buildError(
      "validator.object.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  const keySet = new Set(Object.keys(value));
  result["convert"] = booleanValidator3(
    value["convert"],
    `${propertyPath}.` + `convert`,
    errorList,
  );
  keySet.delete("convert");
  if (keySet.size !== 0) {
    let extraKeys = "";
    for (const k of keySet.keys()) {
      extraKeys += `${k},`;
    }
    return buildError(
      `validator.${parentType}.strict`,
      { propertyPath, extraKeys },
      errorList,
    );
  }
  return result;
}
function objectValidator19(
  value,
  propertyPath,
  errorList = [],
  parentType = "object",
) {
  if (isNil(value)) {
    return buildError(
      "validator.object.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  const keySet = new Set(Object.keys(value));
  result["type"] = stringValidator20(
    value["type"],
    `${propertyPath}.` + `type`,
    errorList,
  );
  keySet.delete("type");
  result["docString"] = stringValidator2(
    value["docString"],
    `${propertyPath}.` + `docString`,
    errorList,
  );
  keySet.delete("docString");
  result["isOptional"] = booleanValidator3(
    value["isOptional"],
    `${propertyPath}.` + `isOptional`,
    errorList,
  );
  keySet.delete("isOptional");
  result["defaultValue"] = anyOfValidator4(
    value["defaultValue"],
    `${propertyPath}.` + `defaultValue`,
    errorList,
  );
  keySet.delete("defaultValue");
  result["uniqueName"] = stringValidator8(
    value["uniqueName"],
    `${propertyPath}.` + `uniqueName`,
    errorList,
  );
  keySet.delete("uniqueName");
  result["group"] = stringValidator8(
    value["group"],
    `${propertyPath}.` + `group`,
    errorList,
  );
  keySet.delete("group");
  result["name"] = stringValidator8(
    value["name"],
    `${propertyPath}.` + `name`,
    errorList,
  );
  keySet.delete("name");
  result["sql"] = objectValidator9(
    value["sql"],
    `${propertyPath}.` + `sql`,
    errorList,
  );
  keySet.delete("sql");
  result["oneOf"] = booleanValidator21(
    value["oneOf"],
    `${propertyPath}.` + `oneOf`,
    errorList,
  );
  keySet.delete("oneOf");
  result["validator"] = objectValidator22(
    value["validator"],
    `${propertyPath}.` + `validator`,
    errorList,
  );
  keySet.delete("validator");
  if (keySet.size !== 0) {
    let extraKeys = "";
    for (const k of keySet.keys()) {
      extraKeys += `${k},`;
    }
    return buildError(
      `validator.${parentType}.strict`,
      { propertyPath, extraKeys },
      errorList,
    );
  }
  return result;
}
function stringValidator24(
  value,
  propertyPath,
  errorList = [],
  parentType = "string",
) {
  if (isNil(value)) {
    return buildError(
      "validator.string.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "string") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (value.length < 1) {
    const min = 1;
    return buildError(
      `validator.${parentType}.min`,
      { propertyPath, min },
      errorList,
    );
  }
  if (value !== "date") {
    const oneOf = "date";
    return buildError(
      `validator.${parentType}.oneOf`,
      { propertyPath, oneOf },
      errorList,
    );
  }
  return value;
}
function objectValidator23(
  value,
  propertyPath,
  errorList = [],
  parentType = "object",
) {
  if (isNil(value)) {
    return buildError(
      "validator.object.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  const keySet = new Set(Object.keys(value));
  result["type"] = stringValidator24(
    value["type"],
    `${propertyPath}.` + `type`,
    errorList,
  );
  keySet.delete("type");
  result["docString"] = stringValidator2(
    value["docString"],
    `${propertyPath}.` + `docString`,
    errorList,
  );
  keySet.delete("docString");
  result["isOptional"] = booleanValidator3(
    value["isOptional"],
    `${propertyPath}.` + `isOptional`,
    errorList,
  );
  keySet.delete("isOptional");
  result["defaultValue"] = anyOfValidator4(
    value["defaultValue"],
    `${propertyPath}.` + `defaultValue`,
    errorList,
  );
  keySet.delete("defaultValue");
  result["uniqueName"] = stringValidator8(
    value["uniqueName"],
    `${propertyPath}.` + `uniqueName`,
    errorList,
  );
  keySet.delete("uniqueName");
  result["group"] = stringValidator8(
    value["group"],
    `${propertyPath}.` + `group`,
    errorList,
  );
  keySet.delete("group");
  result["name"] = stringValidator8(
    value["name"],
    `${propertyPath}.` + `name`,
    errorList,
  );
  keySet.delete("name");
  result["sql"] = objectValidator9(
    value["sql"],
    `${propertyPath}.` + `sql`,
    errorList,
  );
  keySet.delete("sql");
  if (keySet.size !== 0) {
    let extraKeys = "";
    for (const k of keySet.keys()) {
      extraKeys += `${k},`;
    }
    return buildError(
      `validator.${parentType}.strict`,
      { propertyPath, extraKeys },
      errorList,
    );
  }
  return result;
}
function stringValidator26(
  value,
  propertyPath,
  errorList = [],
  parentType = "string",
) {
  if (isNil(value)) {
    return buildError(
      "validator.string.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "string") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (value.length < 1) {
    const min = 1;
    return buildError(
      `validator.${parentType}.min`,
      { propertyPath, min },
      errorList,
    );
  }
  if (value !== "file") {
    const oneOf = "file";
    return buildError(
      `validator.${parentType}.oneOf`,
      { propertyPath, oneOf },
      errorList,
    );
  }
  return value;
}
function objectValidator25(
  value,
  propertyPath,
  errorList = [],
  parentType = "object",
) {
  if (isNil(value)) {
    return buildError(
      "validator.object.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  const keySet = new Set(Object.keys(value));
  result["type"] = stringValidator26(
    value["type"],
    `${propertyPath}.` + `type`,
    errorList,
  );
  keySet.delete("type");
  result["docString"] = stringValidator2(
    value["docString"],
    `${propertyPath}.` + `docString`,
    errorList,
  );
  keySet.delete("docString");
  result["isOptional"] = booleanValidator3(
    value["isOptional"],
    `${propertyPath}.` + `isOptional`,
    errorList,
  );
  keySet.delete("isOptional");
  result["defaultValue"] = anyOfValidator4(
    value["defaultValue"],
    `${propertyPath}.` + `defaultValue`,
    errorList,
  );
  keySet.delete("defaultValue");
  result["uniqueName"] = stringValidator8(
    value["uniqueName"],
    `${propertyPath}.` + `uniqueName`,
    errorList,
  );
  keySet.delete("uniqueName");
  result["group"] = stringValidator8(
    value["group"],
    `${propertyPath}.` + `group`,
    errorList,
  );
  keySet.delete("group");
  result["name"] = stringValidator8(
    value["name"],
    `${propertyPath}.` + `name`,
    errorList,
  );
  keySet.delete("name");
  result["sql"] = objectValidator9(
    value["sql"],
    `${propertyPath}.` + `sql`,
    errorList,
  );
  keySet.delete("sql");
  if (keySet.size !== 0) {
    let extraKeys = "";
    for (const k of keySet.keys()) {
      extraKeys += `${k},`;
    }
    return buildError(
      `validator.${parentType}.strict`,
      { propertyPath, extraKeys },
      errorList,
    );
  }
  return result;
}
function stringValidator28(
  value,
  propertyPath,
  errorList = [],
  parentType = "string",
) {
  if (isNil(value)) {
    return buildError(
      "validator.string.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "string") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (value.length < 1) {
    const min = 1;
    return buildError(
      `validator.${parentType}.min`,
      { propertyPath, min },
      errorList,
    );
  }
  if (value !== "generic") {
    const oneOf = "generic";
    return buildError(
      `validator.${parentType}.oneOf`,
      { propertyPath, oneOf },
      errorList,
    );
  }
  return value;
}
function referenceValidator29(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return buildError(
      "validator.reference.undefined",
      { propertyPath },
      errorList,
    );
  }
  const { errors, data } = codeGenValidators.type(value, propertyPath);
  if (errors !== undefined) {
    errorList.push(...errors);
  }
  return data;
}
function referenceValidator30(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return buildError(
      "validator.reference.undefined",
      { propertyPath },
      errorList,
    );
  }
  const { errors, data } = codeGenValidators.type(value, propertyPath);
  if (errors !== undefined) {
    errorList.push(...errors);
  }
  return data;
}
function objectValidator27(
  value,
  propertyPath,
  errorList = [],
  parentType = "object",
) {
  if (isNil(value)) {
    return buildError(
      "validator.object.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  const keySet = new Set(Object.keys(value));
  result["type"] = stringValidator28(
    value["type"],
    `${propertyPath}.` + `type`,
    errorList,
  );
  keySet.delete("type");
  result["docString"] = stringValidator2(
    value["docString"],
    `${propertyPath}.` + `docString`,
    errorList,
  );
  keySet.delete("docString");
  result["isOptional"] = booleanValidator3(
    value["isOptional"],
    `${propertyPath}.` + `isOptional`,
    errorList,
  );
  keySet.delete("isOptional");
  result["defaultValue"] = anyOfValidator4(
    value["defaultValue"],
    `${propertyPath}.` + `defaultValue`,
    errorList,
  );
  keySet.delete("defaultValue");
  result["uniqueName"] = stringValidator8(
    value["uniqueName"],
    `${propertyPath}.` + `uniqueName`,
    errorList,
  );
  keySet.delete("uniqueName");
  result["group"] = stringValidator8(
    value["group"],
    `${propertyPath}.` + `group`,
    errorList,
  );
  keySet.delete("group");
  result["name"] = stringValidator8(
    value["name"],
    `${propertyPath}.` + `name`,
    errorList,
  );
  keySet.delete("name");
  result["sql"] = objectValidator9(
    value["sql"],
    `${propertyPath}.` + `sql`,
    errorList,
  );
  keySet.delete("sql");
  result["keys"] = referenceValidator29(
    value["keys"],
    `${propertyPath}.` + `keys`,
    errorList,
  );
  keySet.delete("keys");
  result["values"] = referenceValidator30(
    value["values"],
    `${propertyPath}.` + `values`,
    errorList,
  );
  keySet.delete("values");
  if (keySet.size !== 0) {
    let extraKeys = "";
    for (const k of keySet.keys()) {
      extraKeys += `${k},`;
    }
    return buildError(
      `validator.${parentType}.strict`,
      { propertyPath, extraKeys },
      errorList,
    );
  }
  return result;
}
function stringValidator32(
  value,
  propertyPath,
  errorList = [],
  parentType = "string",
) {
  if (isNil(value)) {
    return buildError(
      "validator.string.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "string") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (value.length < 1) {
    const min = 1;
    return buildError(
      `validator.${parentType}.min`,
      { propertyPath, min },
      errorList,
    );
  }
  if (value !== "number") {
    const oneOf = "number";
    return buildError(
      `validator.${parentType}.oneOf`,
      { propertyPath, oneOf },
      errorList,
    );
  }
  return value;
}
function arrayValidator33(
  value,
  propertyPath,
  errorList = [],
  parentType = "array",
) {
  if (isNil(value)) {
    return;
  }
  if (!Array.isArray(value)) {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = [];
  for (let i = 0; i < value.length; ++i) {
    result.push(numberValidator7(value[i], `${propertyPath}[${i}]`, errorList));
  }
  return result;
}
function objectValidator34(
  value,
  propertyPath,
  errorList = [],
  parentType = "object",
) {
  if (isNil(value)) {
    return buildError(
      "validator.object.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  const keySet = new Set(Object.keys(value));
  result["convert"] = booleanValidator3(
    value["convert"],
    `${propertyPath}.` + `convert`,
    errorList,
  );
  keySet.delete("convert");
  result["floatingPoint"] = booleanValidator3(
    value["floatingPoint"],
    `${propertyPath}.` + `floatingPoint`,
    errorList,
  );
  keySet.delete("floatingPoint");
  result["min"] = numberValidator17(
    value["min"],
    `${propertyPath}.` + `min`,
    errorList,
  );
  keySet.delete("min");
  result["max"] = numberValidator17(
    value["max"],
    `${propertyPath}.` + `max`,
    errorList,
  );
  keySet.delete("max");
  if (keySet.size !== 0) {
    let extraKeys = "";
    for (const k of keySet.keys()) {
      extraKeys += `${k},`;
    }
    return buildError(
      `validator.${parentType}.strict`,
      { propertyPath, extraKeys },
      errorList,
    );
  }
  return result;
}
function objectValidator31(
  value,
  propertyPath,
  errorList = [],
  parentType = "object",
) {
  if (isNil(value)) {
    return buildError(
      "validator.object.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  const keySet = new Set(Object.keys(value));
  result["type"] = stringValidator32(
    value["type"],
    `${propertyPath}.` + `type`,
    errorList,
  );
  keySet.delete("type");
  result["docString"] = stringValidator2(
    value["docString"],
    `${propertyPath}.` + `docString`,
    errorList,
  );
  keySet.delete("docString");
  result["isOptional"] = booleanValidator3(
    value["isOptional"],
    `${propertyPath}.` + `isOptional`,
    errorList,
  );
  keySet.delete("isOptional");
  result["defaultValue"] = anyOfValidator4(
    value["defaultValue"],
    `${propertyPath}.` + `defaultValue`,
    errorList,
  );
  keySet.delete("defaultValue");
  result["uniqueName"] = stringValidator8(
    value["uniqueName"],
    `${propertyPath}.` + `uniqueName`,
    errorList,
  );
  keySet.delete("uniqueName");
  result["group"] = stringValidator8(
    value["group"],
    `${propertyPath}.` + `group`,
    errorList,
  );
  keySet.delete("group");
  result["name"] = stringValidator8(
    value["name"],
    `${propertyPath}.` + `name`,
    errorList,
  );
  keySet.delete("name");
  result["sql"] = objectValidator9(
    value["sql"],
    `${propertyPath}.` + `sql`,
    errorList,
  );
  keySet.delete("sql");
  result["oneOf"] = arrayValidator33(
    value["oneOf"],
    `${propertyPath}.` + `oneOf`,
    errorList,
  );
  keySet.delete("oneOf");
  result["validator"] = objectValidator34(
    value["validator"],
    `${propertyPath}.` + `validator`,
    errorList,
  );
  keySet.delete("validator");
  if (keySet.size !== 0) {
    let extraKeys = "";
    for (const k of keySet.keys()) {
      extraKeys += `${k},`;
    }
    return buildError(
      `validator.${parentType}.strict`,
      { propertyPath, extraKeys },
      errorList,
    );
  }
  return result;
}
function stringValidator36(
  value,
  propertyPath,
  errorList = [],
  parentType = "string",
) {
  if (isNil(value)) {
    return buildError(
      "validator.string.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "string") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (value.length < 1) {
    const min = 1;
    return buildError(
      `validator.${parentType}.min`,
      { propertyPath, min },
      errorList,
    );
  }
  if (value !== "object") {
    const oneOf = "object";
    return buildError(
      `validator.${parentType}.oneOf`,
      { propertyPath, oneOf },
      errorList,
    );
  }
  return value;
}
function booleanValidator38(
  value,
  propertyPath,
  errorList = [],
  parentType = "boolean",
) {
  if (isNil(value)) {
    return true;
  }
  if (typeof value !== "boolean") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  return value;
}
function objectValidator37(
  value,
  propertyPath,
  errorList = [],
  parentType = "object",
) {
  if (isNil(value)) {
    return buildError(
      "validator.object.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  const keySet = new Set(Object.keys(value));
  result["strict"] = booleanValidator38(
    value["strict"],
    `${propertyPath}.` + `strict`,
    errorList,
  );
  keySet.delete("strict");
  if (keySet.size !== 0) {
    let extraKeys = "";
    for (const k of keySet.keys()) {
      extraKeys += `${k},`;
    }
    return buildError(
      `validator.${parentType}.strict`,
      { propertyPath, extraKeys },
      errorList,
    );
  }
  return result;
}
function stringValidator40(
  value,
  propertyPath,
  errorList = [],
  parentType = "string",
) {
  if (isNil(value)) {
    return buildError(
      "validator.string.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "string") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (value.length < 1) {
    const min = 1;
    return buildError(
      `validator.${parentType}.min`,
      { propertyPath, min },
      errorList,
    );
  }
  return value;
}
function referenceValidator41(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return buildError(
      "validator.reference.undefined",
      { propertyPath },
      errorList,
    );
  }
  const { errors, data } = codeGenValidators.type(value, propertyPath);
  if (errors !== undefined) {
    errorList.push(...errors);
  }
  return data;
}
function genericValidator39(
  value,
  propertyPath,
  errorList = [],
  parentType = "generic",
) {
  if (isNil(value)) {
    return buildError(
      "validator.generic.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  for (const key of Object.keys(value)) {
    const validatedKey = stringValidator40(
      key,
      `${propertyPath}.$key[${key}]`,
      errorList,
    );
    result[validatedKey] = referenceValidator41(
      value[key],
      `${propertyPath}.$value[${key}]`,
      errorList,
    );
  }
  return result;
}
function objectValidator42(
  value,
  propertyPath,
  errorList = [],
  parentType = "object",
) {
  if (isNil(value)) {
    return;
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  const keySet = new Set(Object.keys(value));
  result["withHistory"] = booleanValidator3(
    value["withHistory"],
    `${propertyPath}.` + `withHistory`,
    errorList,
  );
  keySet.delete("withHistory");
  result["withDates"] = booleanValidator3(
    value["withDates"],
    `${propertyPath}.` + `withDates`,
    errorList,
  );
  keySet.delete("withDates");
  if (keySet.size !== 0) {
    let extraKeys = "";
    for (const k of keySet.keys()) {
      extraKeys += `${k},`;
    }
    return buildError(
      `validator.${parentType}.strict`,
      { propertyPath, extraKeys },
      errorList,
    );
  }
  return result;
}
function objectValidator35(
  value,
  propertyPath,
  errorList = [],
  parentType = "object",
) {
  if (isNil(value)) {
    return buildError(
      "validator.object.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  const keySet = new Set(Object.keys(value));
  result["type"] = stringValidator36(
    value["type"],
    `${propertyPath}.` + `type`,
    errorList,
  );
  keySet.delete("type");
  result["docString"] = stringValidator2(
    value["docString"],
    `${propertyPath}.` + `docString`,
    errorList,
  );
  keySet.delete("docString");
  result["isOptional"] = booleanValidator3(
    value["isOptional"],
    `${propertyPath}.` + `isOptional`,
    errorList,
  );
  keySet.delete("isOptional");
  result["defaultValue"] = anyOfValidator4(
    value["defaultValue"],
    `${propertyPath}.` + `defaultValue`,
    errorList,
  );
  keySet.delete("defaultValue");
  result["uniqueName"] = stringValidator8(
    value["uniqueName"],
    `${propertyPath}.` + `uniqueName`,
    errorList,
  );
  keySet.delete("uniqueName");
  result["group"] = stringValidator8(
    value["group"],
    `${propertyPath}.` + `group`,
    errorList,
  );
  keySet.delete("group");
  result["name"] = stringValidator8(
    value["name"],
    `${propertyPath}.` + `name`,
    errorList,
  );
  keySet.delete("name");
  result["sql"] = objectValidator9(
    value["sql"],
    `${propertyPath}.` + `sql`,
    errorList,
  );
  keySet.delete("sql");
  result["validator"] = objectValidator37(
    value["validator"],
    `${propertyPath}.` + `validator`,
    errorList,
  );
  keySet.delete("validator");
  result["keys"] = genericValidator39(
    value["keys"],
    `${propertyPath}.` + `keys`,
    errorList,
  );
  keySet.delete("keys");
  result["enableQueries"] = booleanValidator3(
    value["enableQueries"],
    `${propertyPath}.` + `enableQueries`,
    errorList,
  );
  keySet.delete("enableQueries");
  result["queryOptions"] = objectValidator42(
    value["queryOptions"],
    `${propertyPath}.` + `queryOptions`,
    errorList,
  );
  keySet.delete("queryOptions");
  if (keySet.size !== 0) {
    let extraKeys = "";
    for (const k of keySet.keys()) {
      extraKeys += `${k},`;
    }
    return buildError(
      `validator.${parentType}.strict`,
      { propertyPath, extraKeys },
      errorList,
    );
  }
  return result;
}
function stringValidator44(
  value,
  propertyPath,
  errorList = [],
  parentType = "string",
) {
  if (isNil(value)) {
    return buildError(
      "validator.string.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "string") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (value.length < 1) {
    const min = 1;
    return buildError(
      `validator.${parentType}.min`,
      { propertyPath, min },
      errorList,
    );
  }
  if (value !== "reference") {
    const oneOf = "reference";
    return buildError(
      `validator.${parentType}.oneOf`,
      { propertyPath, oneOf },
      errorList,
    );
  }
  return value;
}
function objectValidator45(
  value,
  propertyPath,
  errorList = [],
  parentType = "object",
) {
  if (isNil(value)) {
    return buildError(
      "validator.object.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  const keySet = new Set(Object.keys(value));
  result["uniqueName"] = stringValidator8(
    value["uniqueName"],
    `${propertyPath}.` + `uniqueName`,
    errorList,
  );
  keySet.delete("uniqueName");
  result["group"] = stringValidator8(
    value["group"],
    `${propertyPath}.` + `group`,
    errorList,
  );
  keySet.delete("group");
  result["name"] = stringValidator8(
    value["name"],
    `${propertyPath}.` + `name`,
    errorList,
  );
  keySet.delete("name");
  if (keySet.size !== 0) {
    let extraKeys = "";
    for (const k of keySet.keys()) {
      extraKeys += `${k},`;
    }
    return buildError(
      `validator.${parentType}.strict`,
      { propertyPath, extraKeys },
      errorList,
    );
  }
  return result;
}
function objectValidator43(
  value,
  propertyPath,
  errorList = [],
  parentType = "object",
) {
  if (isNil(value)) {
    return buildError(
      "validator.object.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  const keySet = new Set(Object.keys(value));
  result["type"] = stringValidator44(
    value["type"],
    `${propertyPath}.` + `type`,
    errorList,
  );
  keySet.delete("type");
  result["docString"] = stringValidator2(
    value["docString"],
    `${propertyPath}.` + `docString`,
    errorList,
  );
  keySet.delete("docString");
  result["isOptional"] = booleanValidator3(
    value["isOptional"],
    `${propertyPath}.` + `isOptional`,
    errorList,
  );
  keySet.delete("isOptional");
  result["defaultValue"] = anyOfValidator4(
    value["defaultValue"],
    `${propertyPath}.` + `defaultValue`,
    errorList,
  );
  keySet.delete("defaultValue");
  result["uniqueName"] = stringValidator8(
    value["uniqueName"],
    `${propertyPath}.` + `uniqueName`,
    errorList,
  );
  keySet.delete("uniqueName");
  result["group"] = stringValidator8(
    value["group"],
    `${propertyPath}.` + `group`,
    errorList,
  );
  keySet.delete("group");
  result["name"] = stringValidator8(
    value["name"],
    `${propertyPath}.` + `name`,
    errorList,
  );
  keySet.delete("name");
  result["sql"] = objectValidator9(
    value["sql"],
    `${propertyPath}.` + `sql`,
    errorList,
  );
  keySet.delete("sql");
  result["reference"] = objectValidator45(
    value["reference"],
    `${propertyPath}.` + `reference`,
    errorList,
  );
  keySet.delete("reference");
  if (keySet.size !== 0) {
    let extraKeys = "";
    for (const k of keySet.keys()) {
      extraKeys += `${k},`;
    }
    return buildError(
      `validator.${parentType}.strict`,
      { propertyPath, extraKeys },
      errorList,
    );
  }
  return result;
}
function stringValidator47(
  value,
  propertyPath,
  errorList = [],
  parentType = "string",
) {
  if (isNil(value)) {
    return buildError(
      "validator.string.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "string") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (value.length < 1) {
    const min = 1;
    return buildError(
      `validator.${parentType}.min`,
      { propertyPath, min },
      errorList,
    );
  }
  if (value !== "relation") {
    const oneOf = "relation";
    return buildError(
      `validator.${parentType}.oneOf`,
      { propertyPath, oneOf },
      errorList,
    );
  }
  return value;
}
function stringValidator48(
  value,
  propertyPath,
  errorList = [],
  parentType = "string",
) {
  if (isNil(value)) {
    return buildError(
      "validator.string.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "string") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (value.length < 1) {
    const min = 1;
    return buildError(
      `validator.${parentType}.min`,
      { propertyPath, min },
      errorList,
    );
  }
  if (value !== "oneToOne" && value !== "oneToMany" && value !== "manyToOne") {
    const oneOf = "oneToOne, oneToMany, manyToOne";
    return buildError(
      `validator.${parentType}.oneOf`,
      { propertyPath, oneOf },
      errorList,
    );
  }
  return value;
}
function referenceValidator49(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return buildError(
      "validator.reference.undefined",
      { propertyPath },
      errorList,
    );
  }
  const { errors, data } = codeGenValidators.type(value, propertyPath);
  if (errors !== undefined) {
    errorList.push(...errors);
  }
  return data;
}
function referenceValidator50(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return buildError(
      "validator.reference.undefined",
      { propertyPath },
      errorList,
    );
  }
  const { errors, data } = codeGenValidators.type(value, propertyPath);
  if (errors !== undefined) {
    errorList.push(...errors);
  }
  return data;
}
function objectValidator46(
  value,
  propertyPath,
  errorList = [],
  parentType = "object",
) {
  if (isNil(value)) {
    return buildError(
      "validator.object.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  const keySet = new Set(Object.keys(value));
  result["type"] = stringValidator47(
    value["type"],
    `${propertyPath}.` + `type`,
    errorList,
  );
  keySet.delete("type");
  result["docString"] = stringValidator2(
    value["docString"],
    `${propertyPath}.` + `docString`,
    errorList,
  );
  keySet.delete("docString");
  result["isOptional"] = booleanValidator3(
    value["isOptional"],
    `${propertyPath}.` + `isOptional`,
    errorList,
  );
  keySet.delete("isOptional");
  result["defaultValue"] = anyOfValidator4(
    value["defaultValue"],
    `${propertyPath}.` + `defaultValue`,
    errorList,
  );
  keySet.delete("defaultValue");
  result["uniqueName"] = stringValidator8(
    value["uniqueName"],
    `${propertyPath}.` + `uniqueName`,
    errorList,
  );
  keySet.delete("uniqueName");
  result["group"] = stringValidator8(
    value["group"],
    `${propertyPath}.` + `group`,
    errorList,
  );
  keySet.delete("group");
  result["name"] = stringValidator8(
    value["name"],
    `${propertyPath}.` + `name`,
    errorList,
  );
  keySet.delete("name");
  result["sql"] = objectValidator9(
    value["sql"],
    `${propertyPath}.` + `sql`,
    errorList,
  );
  keySet.delete("sql");
  result["relationType"] = stringValidator48(
    value["relationType"],
    `${propertyPath}.` + `relationType`,
    errorList,
  );
  keySet.delete("relationType");
  result["left"] = referenceValidator49(
    value["left"],
    `${propertyPath}.` + `left`,
    errorList,
  );
  keySet.delete("left");
  result["leftKey"] = stringValidator40(
    value["leftKey"],
    `${propertyPath}.` + `leftKey`,
    errorList,
  );
  keySet.delete("leftKey");
  result["right"] = referenceValidator50(
    value["right"],
    `${propertyPath}.` + `right`,
    errorList,
  );
  keySet.delete("right");
  result["rightKey"] = stringValidator40(
    value["rightKey"],
    `${propertyPath}.` + `rightKey`,
    errorList,
  );
  keySet.delete("rightKey");
  result["substituteKey"] = stringValidator40(
    value["substituteKey"],
    `${propertyPath}.` + `substituteKey`,
    errorList,
  );
  keySet.delete("substituteKey");
  if (keySet.size !== 0) {
    let extraKeys = "";
    for (const k of keySet.keys()) {
      extraKeys += `${k},`;
    }
    return buildError(
      `validator.${parentType}.strict`,
      { propertyPath, extraKeys },
      errorList,
    );
  }
  return result;
}
function stringValidator52(
  value,
  propertyPath,
  errorList = [],
  parentType = "string",
) {
  if (isNil(value)) {
    return buildError(
      "validator.string.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "string") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (value.length < 1) {
    const min = 1;
    return buildError(
      `validator.${parentType}.min`,
      { propertyPath, min },
      errorList,
    );
  }
  if (value !== "route") {
    const oneOf = "route";
    return buildError(
      `validator.${parentType}.oneOf`,
      { propertyPath, oneOf },
      errorList,
    );
  }
  return value;
}
function stringValidator53(
  value,
  propertyPath,
  errorList = [],
  parentType = "string",
) {
  if (isNil(value)) {
    return buildError(
      "validator.string.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "string") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (value.length < 1) {
    const min = 1;
    return buildError(
      `validator.${parentType}.min`,
      { propertyPath, min },
      errorList,
    );
  }
  if (
    value !== "GET" &&
    value !== "POST" &&
    value !== "PUT" &&
    value !== "DELETE" &&
    value !== "HEAD"
  ) {
    const oneOf = "GET, POST, PUT, DELETE, HEAD";
    return buildError(
      `validator.${parentType}.oneOf`,
      { propertyPath, oneOf },
      errorList,
    );
  }
  return value;
}
function arrayValidator54(
  value,
  propertyPath,
  errorList = [],
  parentType = "array",
) {
  if (isNil(value)) {
    return buildError("validator.array.undefined", { propertyPath }, errorList);
  }
  if (!Array.isArray(value)) {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = [];
  for (let i = 0; i < value.length; ++i) {
    result.push(
      stringValidator40(value[i], `${propertyPath}[${i}]`, errorList),
    );
  }
  return result;
}
function referenceValidator55(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return;
  }
  const { errors, data } = codeGenValidators.type(value, propertyPath);
  if (errors !== undefined) {
    errorList.push(...errors);
  }
  return data;
}
function referenceValidator56(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return;
  }
  const { errors, data } = codeGenValidators.type(value, propertyPath);
  if (errors !== undefined) {
    errorList.push(...errors);
  }
  return data;
}
function referenceValidator57(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return;
  }
  const { errors, data } = codeGenValidators.type(value, propertyPath);
  if (errors !== undefined) {
    errorList.push(...errors);
  }
  return data;
}
function referenceValidator58(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return;
  }
  const { errors, data } = codeGenValidators.type(value, propertyPath);
  if (errors !== undefined) {
    errorList.push(...errors);
  }
  return data;
}
function referenceValidator59(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return;
  }
  const { errors, data } = codeGenValidators.type(value, propertyPath);
  if (errors !== undefined) {
    errorList.push(...errors);
  }
  return data;
}
function objectValidator51(
  value,
  propertyPath,
  errorList = [],
  parentType = "object",
) {
  if (isNil(value)) {
    return buildError(
      "validator.object.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  const keySet = new Set(Object.keys(value));
  result["type"] = stringValidator52(
    value["type"],
    `${propertyPath}.` + `type`,
    errorList,
  );
  keySet.delete("type");
  result["docString"] = stringValidator2(
    value["docString"],
    `${propertyPath}.` + `docString`,
    errorList,
  );
  keySet.delete("docString");
  result["isOptional"] = booleanValidator3(
    value["isOptional"],
    `${propertyPath}.` + `isOptional`,
    errorList,
  );
  keySet.delete("isOptional");
  result["defaultValue"] = anyOfValidator4(
    value["defaultValue"],
    `${propertyPath}.` + `defaultValue`,
    errorList,
  );
  keySet.delete("defaultValue");
  result["uniqueName"] = stringValidator8(
    value["uniqueName"],
    `${propertyPath}.` + `uniqueName`,
    errorList,
  );
  keySet.delete("uniqueName");
  result["group"] = stringValidator8(
    value["group"],
    `${propertyPath}.` + `group`,
    errorList,
  );
  keySet.delete("group");
  result["name"] = stringValidator8(
    value["name"],
    `${propertyPath}.` + `name`,
    errorList,
  );
  keySet.delete("name");
  result["sql"] = objectValidator9(
    value["sql"],
    `${propertyPath}.` + `sql`,
    errorList,
  );
  keySet.delete("sql");
  result["method"] = stringValidator53(
    value["method"],
    `${propertyPath}.` + `method`,
    errorList,
  );
  keySet.delete("method");
  result["path"] = stringValidator40(
    value["path"],
    `${propertyPath}.` + `path`,
    errorList,
  );
  keySet.delete("path");
  result["tags"] = arrayValidator54(
    value["tags"],
    `${propertyPath}.` + `tags`,
    errorList,
  );
  keySet.delete("tags");
  result["query"] = referenceValidator55(
    value["query"],
    `${propertyPath}.` + `query`,
    errorList,
  );
  keySet.delete("query");
  result["params"] = referenceValidator56(
    value["params"],
    `${propertyPath}.` + `params`,
    errorList,
  );
  keySet.delete("params");
  result["body"] = referenceValidator57(
    value["body"],
    `${propertyPath}.` + `body`,
    errorList,
  );
  keySet.delete("body");
  result["files"] = referenceValidator58(
    value["files"],
    `${propertyPath}.` + `files`,
    errorList,
  );
  keySet.delete("files");
  result["response"] = referenceValidator59(
    value["response"],
    `${propertyPath}.` + `response`,
    errorList,
  );
  keySet.delete("response");
  if (keySet.size !== 0) {
    let extraKeys = "";
    for (const k of keySet.keys()) {
      extraKeys += `${k},`;
    }
    return buildError(
      `validator.${parentType}.strict`,
      { propertyPath, extraKeys },
      errorList,
    );
  }
  return result;
}
function stringValidator61(
  value,
  propertyPath,
  errorList = [],
  parentType = "string",
) {
  if (isNil(value)) {
    return buildError(
      "validator.string.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "string") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (value.length < 1) {
    const min = 1;
    return buildError(
      `validator.${parentType}.min`,
      { propertyPath, min },
      errorList,
    );
  }
  if (value !== "string") {
    const oneOf = "string";
    return buildError(
      `validator.${parentType}.oneOf`,
      { propertyPath, oneOf },
      errorList,
    );
  }
  return value;
}
function arrayValidator62(
  value,
  propertyPath,
  errorList = [],
  parentType = "array",
) {
  if (isNil(value)) {
    return;
  }
  if (!Array.isArray(value)) {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = [];
  for (let i = 0; i < value.length; ++i) {
    result.push(
      stringValidator40(value[i], `${propertyPath}[${i}]`, errorList),
    );
  }
  return result;
}
function numberValidator64(
  value,
  propertyPath,
  errorList = [],
  parentType = "number",
) {
  if (isNil(value)) {
    return 1;
  }
  if (typeof value !== "number" || isNaN(value) || !isFinite(value)) {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (!Number.isInteger(value)) {
    return buildError(
      `validator.${parentType}.integer`,
      { propertyPath },
      errorList,
    );
  }
  return value;
}
function objectValidator63(
  value,
  propertyPath,
  errorList = [],
  parentType = "object",
) {
  if (isNil(value)) {
    return buildError(
      "validator.object.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  const keySet = new Set(Object.keys(value));
  result["convert"] = booleanValidator3(
    value["convert"],
    `${propertyPath}.` + `convert`,
    errorList,
  );
  keySet.delete("convert");
  result["trim"] = booleanValidator3(
    value["trim"],
    `${propertyPath}.` + `trim`,
    errorList,
  );
  keySet.delete("trim");
  result["lowerCase"] = booleanValidator3(
    value["lowerCase"],
    `${propertyPath}.` + `lowerCase`,
    errorList,
  );
  keySet.delete("lowerCase");
  result["upperCase"] = booleanValidator3(
    value["upperCase"],
    `${propertyPath}.` + `upperCase`,
    errorList,
  );
  keySet.delete("upperCase");
  result["min"] = numberValidator64(
    value["min"],
    `${propertyPath}.` + `min`,
    errorList,
  );
  keySet.delete("min");
  result["max"] = numberValidator17(
    value["max"],
    `${propertyPath}.` + `max`,
    errorList,
  );
  keySet.delete("max");
  result["pattern"] = stringValidator8(
    value["pattern"],
    `${propertyPath}.` + `pattern`,
    errorList,
  );
  keySet.delete("pattern");
  if (keySet.size !== 0) {
    let extraKeys = "";
    for (const k of keySet.keys()) {
      extraKeys += `${k},`;
    }
    return buildError(
      `validator.${parentType}.strict`,
      { propertyPath, extraKeys },
      errorList,
    );
  }
  return result;
}
function objectValidator60(
  value,
  propertyPath,
  errorList = [],
  parentType = "object",
) {
  if (isNil(value)) {
    return buildError(
      "validator.object.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  const keySet = new Set(Object.keys(value));
  result["type"] = stringValidator61(
    value["type"],
    `${propertyPath}.` + `type`,
    errorList,
  );
  keySet.delete("type");
  result["docString"] = stringValidator2(
    value["docString"],
    `${propertyPath}.` + `docString`,
    errorList,
  );
  keySet.delete("docString");
  result["isOptional"] = booleanValidator3(
    value["isOptional"],
    `${propertyPath}.` + `isOptional`,
    errorList,
  );
  keySet.delete("isOptional");
  result["defaultValue"] = anyOfValidator4(
    value["defaultValue"],
    `${propertyPath}.` + `defaultValue`,
    errorList,
  );
  keySet.delete("defaultValue");
  result["uniqueName"] = stringValidator8(
    value["uniqueName"],
    `${propertyPath}.` + `uniqueName`,
    errorList,
  );
  keySet.delete("uniqueName");
  result["group"] = stringValidator8(
    value["group"],
    `${propertyPath}.` + `group`,
    errorList,
  );
  keySet.delete("group");
  result["name"] = stringValidator8(
    value["name"],
    `${propertyPath}.` + `name`,
    errorList,
  );
  keySet.delete("name");
  result["sql"] = objectValidator9(
    value["sql"],
    `${propertyPath}.` + `sql`,
    errorList,
  );
  keySet.delete("sql");
  result["oneOf"] = arrayValidator62(
    value["oneOf"],
    `${propertyPath}.` + `oneOf`,
    errorList,
  );
  keySet.delete("oneOf");
  result["validator"] = objectValidator63(
    value["validator"],
    `${propertyPath}.` + `validator`,
    errorList,
  );
  keySet.delete("validator");
  if (keySet.size !== 0) {
    let extraKeys = "";
    for (const k of keySet.keys()) {
      extraKeys += `${k},`;
    }
    return buildError(
      `validator.${parentType}.strict`,
      { propertyPath, extraKeys },
      errorList,
    );
  }
  return result;
}
function referenceValidator67(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return buildError(
      "validator.reference.undefined",
      { propertyPath },
      errorList,
    );
  }
  const { errors, data } = codeGenValidators.type(value, propertyPath);
  if (errors !== undefined) {
    errorList.push(...errors);
  }
  return data;
}
function genericValidator66(
  value,
  propertyPath,
  errorList = [],
  parentType = "generic",
) {
  if (isNil(value)) {
    return buildError(
      "validator.generic.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  for (const key of Object.keys(value)) {
    const validatedKey = stringValidator40(
      key,
      `${propertyPath}.$key[${key}]`,
      errorList,
    );
    result[validatedKey] = referenceValidator67(
      value[key],
      `${propertyPath}.$value[${key}]`,
      errorList,
    );
  }
  return result;
}
function genericValidator65(
  value,
  propertyPath,
  errorList = [],
  parentType = "generic",
) {
  if (isNil(value)) {
    return buildError(
      "validator.generic.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  for (const key of Object.keys(value)) {
    const validatedKey = stringValidator40(
      key,
      `${propertyPath}.$key[${key}]`,
      errorList,
    );
    result[validatedKey] = genericValidator66(
      value[key],
      `${propertyPath}.$value[${key}]`,
      errorList,
    );
  }
  return result;
}
function referenceValidator69(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return buildError(
      "validator.reference.undefined",
      { propertyPath },
      errorList,
    );
  }
  const { errors, data } = codeGenValidators.anyType(value, propertyPath);
  if (errors !== undefined) {
    errorList.push(...errors);
  }
  return data;
}
function referenceValidator70(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return buildError(
      "validator.reference.undefined",
      { propertyPath },
      errorList,
    );
  }
  const { errors, data } = codeGenValidators.anyOfType(value, propertyPath);
  if (errors !== undefined) {
    errorList.push(...errors);
  }
  return data;
}
function referenceValidator71(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return buildError(
      "validator.reference.undefined",
      { propertyPath },
      errorList,
    );
  }
  const { errors, data } = codeGenValidators.arrayType(value, propertyPath);
  if (errors !== undefined) {
    errorList.push(...errors);
  }
  return data;
}
function referenceValidator72(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return buildError(
      "validator.reference.undefined",
      { propertyPath },
      errorList,
    );
  }
  const { errors, data } = codeGenValidators.booleanType(value, propertyPath);
  if (errors !== undefined) {
    errorList.push(...errors);
  }
  return data;
}
function referenceValidator73(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return buildError(
      "validator.reference.undefined",
      { propertyPath },
      errorList,
    );
  }
  const { errors, data } = codeGenValidators.dateType(value, propertyPath);
  if (errors !== undefined) {
    errorList.push(...errors);
  }
  return data;
}
function referenceValidator74(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return buildError(
      "validator.reference.undefined",
      { propertyPath },
      errorList,
    );
  }
  const { errors, data } = codeGenValidators.fileType(value, propertyPath);
  if (errors !== undefined) {
    errorList.push(...errors);
  }
  return data;
}
function referenceValidator75(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return buildError(
      "validator.reference.undefined",
      { propertyPath },
      errorList,
    );
  }
  const { errors, data } = codeGenValidators.genericType(value, propertyPath);
  if (errors !== undefined) {
    errorList.push(...errors);
  }
  return data;
}
function referenceValidator76(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return buildError(
      "validator.reference.undefined",
      { propertyPath },
      errorList,
    );
  }
  const { errors, data } = codeGenValidators.numberType(value, propertyPath);
  if (errors !== undefined) {
    errorList.push(...errors);
  }
  return data;
}
function referenceValidator77(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return buildError(
      "validator.reference.undefined",
      { propertyPath },
      errorList,
    );
  }
  const { errors, data } = codeGenValidators.objectType(value, propertyPath);
  if (errors !== undefined) {
    errorList.push(...errors);
  }
  return data;
}
function referenceValidator78(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return buildError(
      "validator.reference.undefined",
      { propertyPath },
      errorList,
    );
  }
  const { errors, data } = codeGenValidators.referenceType(value, propertyPath);
  if (errors !== undefined) {
    errorList.push(...errors);
  }
  return data;
}
function referenceValidator79(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return buildError(
      "validator.reference.undefined",
      { propertyPath },
      errorList,
    );
  }
  const { errors, data } = codeGenValidators.relationType(value, propertyPath);
  if (errors !== undefined) {
    errorList.push(...errors);
  }
  return data;
}
function referenceValidator80(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return buildError(
      "validator.reference.undefined",
      { propertyPath },
      errorList,
    );
  }
  const { errors, data } = codeGenValidators.stringType(value, propertyPath);
  if (errors !== undefined) {
    errorList.push(...errors);
  }
  return data;
}
function referenceValidator81(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return buildError(
      "validator.reference.undefined",
      { propertyPath },
      errorList,
    );
  }
  const { errors, data } = codeGenValidators.uuidType(value, propertyPath);
  if (errors !== undefined) {
    errorList.push(...errors);
  }
  return data;
}
function referenceValidator82(
  value,
  propertyPath,
  errorList = [],
  parentType = "reference",
) {
  if (isNil(value)) {
    return buildError(
      "validator.reference.undefined",
      { propertyPath },
      errorList,
    );
  }
  const { errors, data } = codeGenValidators.routeType(value, propertyPath);
  if (errors !== undefined) {
    errorList.push(...errors);
  }
  return data;
}
function anyOfValidator68(
  value,
  propertyPath,
  errorList = [],
  parentType = "anyOf",
) {
  if (isNil(value)) {
    return buildError("validator.anyOf.undefined", { propertyPath }, errorList);
  }
  let errCount = 0;
  const errors = [];
  let result = undefined;
  result = referenceValidator69(value, propertyPath, errors);
  if (errors.length === errCount) {
    return result;
  }
  errCount = errors.length;
  result = referenceValidator70(value, propertyPath, errors);
  if (errors.length === errCount) {
    return result;
  }
  errCount = errors.length;
  result = referenceValidator71(value, propertyPath, errors);
  if (errors.length === errCount) {
    return result;
  }
  errCount = errors.length;
  result = referenceValidator72(value, propertyPath, errors);
  if (errors.length === errCount) {
    return result;
  }
  errCount = errors.length;
  result = referenceValidator73(value, propertyPath, errors);
  if (errors.length === errCount) {
    return result;
  }
  errCount = errors.length;
  result = referenceValidator74(value, propertyPath, errors);
  if (errors.length === errCount) {
    return result;
  }
  errCount = errors.length;
  result = referenceValidator75(value, propertyPath, errors);
  if (errors.length === errCount) {
    return result;
  }
  errCount = errors.length;
  result = referenceValidator76(value, propertyPath, errors);
  if (errors.length === errCount) {
    return result;
  }
  errCount = errors.length;
  result = referenceValidator77(value, propertyPath, errors);
  if (errors.length === errCount) {
    return result;
  }
  errCount = errors.length;
  result = referenceValidator78(value, propertyPath, errors);
  if (errors.length === errCount) {
    return result;
  }
  errCount = errors.length;
  result = referenceValidator79(value, propertyPath, errors);
  if (errors.length === errCount) {
    return result;
  }
  errCount = errors.length;
  result = referenceValidator80(value, propertyPath, errors);
  if (errors.length === errCount) {
    return result;
  }
  errCount = errors.length;
  result = referenceValidator81(value, propertyPath, errors);
  if (errors.length === errCount) {
    return result;
  }
  errCount = errors.length;
  result = referenceValidator82(value, propertyPath, errors);
  if (errors.length === errCount) {
    return result;
  }
  errCount = errors.length;
  return buildError(
    `validator.${parentType}.type`,
    { propertyPath, errors },
    errorList,
  );
}
function stringValidator84(
  value,
  propertyPath,
  errorList = [],
  parentType = "string",
) {
  if (isNil(value)) {
    return buildError(
      "validator.string.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "string") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  if (value.length < 1) {
    const min = 1;
    return buildError(
      `validator.${parentType}.min`,
      { propertyPath, min },
      errorList,
    );
  }
  if (value !== "uuid") {
    const oneOf = "uuid";
    return buildError(
      `validator.${parentType}.oneOf`,
      { propertyPath, oneOf },
      errorList,
    );
  }
  return value;
}
function objectValidator83(
  value,
  propertyPath,
  errorList = [],
  parentType = "object",
) {
  if (isNil(value)) {
    return buildError(
      "validator.object.undefined",
      { propertyPath },
      errorList,
    );
  }
  if (typeof value !== "object") {
    return buildError(
      `validator.${parentType}.type`,
      { propertyPath },
      errorList,
    );
  }
  const result = Object.create(null);
  const keySet = new Set(Object.keys(value));
  result["type"] = stringValidator84(
    value["type"],
    `${propertyPath}.` + `type`,
    errorList,
  );
  keySet.delete("type");
  result["docString"] = stringValidator2(
    value["docString"],
    `${propertyPath}.` + `docString`,
    errorList,
  );
  keySet.delete("docString");
  result["isOptional"] = booleanValidator3(
    value["isOptional"],
    `${propertyPath}.` + `isOptional`,
    errorList,
  );
  keySet.delete("isOptional");
  result["defaultValue"] = anyOfValidator4(
    value["defaultValue"],
    `${propertyPath}.` + `defaultValue`,
    errorList,
  );
  keySet.delete("defaultValue");
  result["uniqueName"] = stringValidator8(
    value["uniqueName"],
    `${propertyPath}.` + `uniqueName`,
    errorList,
  );
  keySet.delete("uniqueName");
  result["group"] = stringValidator8(
    value["group"],
    `${propertyPath}.` + `group`,
    errorList,
  );
  keySet.delete("group");
  result["name"] = stringValidator8(
    value["name"],
    `${propertyPath}.` + `name`,
    errorList,
  );
  keySet.delete("name");
  result["sql"] = objectValidator9(
    value["sql"],
    `${propertyPath}.` + `sql`,
    errorList,
  );
  keySet.delete("sql");
  if (keySet.size !== 0) {
    let extraKeys = "";
    for (const k of keySet.keys()) {
      extraKeys += `${k},`;
    }
    return buildError(
      `validator.${parentType}.strict`,
      { propertyPath, extraKeys },
      errorList,
    );
  }
  return result;
}
