{{ if (field.stringify) { }}
JSON.stringify(it.{{= field.source }} ?? {{= field.defaultValue || "undefined" }})
{{ } else { }}
it.{{= field.source }} ?? {{= field.defaultValue || "undefined" }}
{{ } }}
