{
{{ for (const field of item.partialFields) { if (field.stringify) { }}
    {{= field.result }}: JSON.stringify(it.{{= field.source }} ?? {{= field.defaultValue || "undefined" }}), {{ } else { }} {{= field.result }}: it.{{= field.source }} ?? {{= field.defaultValue || "undefined" }},
{{ } } }}
}
