{{ if (ref.queryOptions?.withDates || ref.queryOptions?.withHistory) { }}
ORDER BY {{= item.shortName }}."createdAt", {{= item.shortName }}."updatedAt" {{= ref.keys["id"] !== undefined ? `, ${item.shortName}."id"` : "" }}
{{ } }}
