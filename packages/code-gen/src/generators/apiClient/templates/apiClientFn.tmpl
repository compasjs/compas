{{ let hasParam = model.params !== undefined; }}
{{ const url = model.path.split("/").map(part => { if (part.startsWith(":")) { hasParam = true; return "${params." + part.substring(1) + "}"; } else { return part; } }).join("/"); }}

/**
 * {{= model.uniqueName }}
 * Tags: {{= model.tags.join(", ") }}
 * Docs: {{= model.docString }}
{{ if (hasParam) { }}
 * @param { {{= model.params ? model.params.reference.uniqueName + "_Input" : "object" }}} params
{{ } }}
{{ if (model.query) { }}
 * @param { {{= model.query.reference.uniqueName }}_Input} query
{{ } }}
{{ if (model.body) { }}
 * @param { {{= model.body.reference.uniqueName }}_Input} body
{{ } }}
{{ if (model.response) { }}
 * @returns {Promise.<{{= model.response.reference.uniqueName }}>}
{{ } else { }}
 * @returns {Promise.<*>}
{{ } }}

 */
{{= model.name }}: async function (
{{ if (hasParam) { }}
params,
{{ } }}
{{ if (model.query) { }}
query,
{{ } }}
{{ if (model.body) { }}
body,
{{ } }}
) {
    checkApiClient();

    {{ if (options.enableMocks && model.response !== undefined) { }}
      try {
        const response = await _internalClient.request({
          url: `{{= url }}`,
          method: "{{= model.method.toLowerCase() }}",
          params: {{ if (model.query) { }}query{{ } else { }}{}{{ } }},
          data: {{ if (model.body) { }}body{{ } else { }}{}{{ } }}
        });

        return response.data;
      } catch (e) {
        if (e.response && e.response.status === 405) {
          return {{= model.response.reference.group }}Mocks.{{= model.response.reference.name }}();
        }
        throw e;
      }
    {{ } else { }}
        const response = await _internalClient.request({
          url: `{{= url }}`,
          method: "{{= model.method.toLowerCase() }}",
          params: {{ if (model.query) { }}query{{ } else { }}{}{{ } }},
          data: {{ if (model.body) { }}body{{ } else { }}{}{{ } }}
        });

        return response.data;
    {{ } }}
},
((newline))
