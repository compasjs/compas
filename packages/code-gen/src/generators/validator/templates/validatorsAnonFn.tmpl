{{ if (model.docString !== "") { }}
/**
 * {{= model.docString }}
 */
{{ } }}
function {{= model.type}}Validator{{= ctx.counter++ }}(
  value,
  propertyPath,
  errorList = [],
  parentType = "{{= model.type }}"
  ) {
  if (isNil(value)) {
    {{ if (model.isOptional) { }}
      return {{= model.defaultValue }};
    {{ } else { }}
      return buildError("validator.{{= model.type }}.undefined", { propertyPath }, errorList);
    {{ } }}
  }

  {{ /* makes it easier for type implementations */ }}
  {{ model.validator = model.validator || {}; }}

  {{= validatorExec({ type: model.type, model, ctx, options, }).trim() }}
}
((newline))

