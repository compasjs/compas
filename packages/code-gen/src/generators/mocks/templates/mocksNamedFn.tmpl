/**
 * Generated mock for {{= model.uniqueName }}
 * @return { {{= model.uniqueName }}}
 */
{{= lowerCaseFirst(model.name) }}: () => {
  return {{= mockExec({ type: model.type, model, ignoreDefaults: false }).trim() }};
},

/**
 * Generated mock for {{= model.uniqueName }} ignoring default values
 * @return { {{= model.uniqueName }}_Optional}
 */
{{= lowerCaseFirst(model.name) }}Optional: () => {
  return {{= mockExec({ type: model.type, model, ignoreDefaults: true }).trim() }};
},
