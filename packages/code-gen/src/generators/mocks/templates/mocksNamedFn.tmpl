/**
 * Generated mock for {{= model.uniqueName }}
 * @return { {{= model.uniqueName }}}
 */
{{= model.name }}: () => {
  return {{= mockExec({ type: model.type, model, isInputType: false }).trim() }};
},
((newline))

/**
 * Generated mock for {{= model.uniqueName }} ignoring default values
 * @return { {{= model.uniqueName }}_Input}
 */
{{= model.name }}Input: () => {
  return {{= mockExec({ type: model.type, model, isInputType: true }).trim() }};
},
((newline))
