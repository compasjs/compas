{{= options.fileHeader }}
((newline))

import chance from "chance";
((newline))


let _mocker = new chance.Chance();
((newline))

export function setMocker(mocker) {
  if (!(mocker instanceof chance.Chance)) {
    throw new Error("Not instance of Chance.js");
  }

  _mocker = mocker;
}
((newline))

{{ for (const groupName of Object.keys(structure)) { }}

  export const {{= groupName }}Mocks = {

  {{ for (const itemName of Object.keys(structure[groupName])) { }}
    {{ const item = structure[groupName][itemName]; }}

    {{ if (options.mock_enabledTypes.indexOf(item.type) === -1) { }}
    {{ continue; }}
    {{ } }}
  ((newline))

    {{= mocksNamedFn({ model: item }) }}

  {{ } }}

  };
((newline))

{{ } }}
