import { isNil } from "@lbu/stdlib";
import { AppError } from "@lbu/server";

export const validatorHooks = {};

{{ for (const v of it.validatorFunctions) { }}
{{= validatorsNamedFn({ models, ...v }) }}
{{ } }}
