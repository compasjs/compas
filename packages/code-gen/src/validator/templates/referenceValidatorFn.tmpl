const {{= functionName }} = (value, propertyPath) => {
  if (isNil(value)) {
    {{ if (it.optional) { }}
      return undefined;
    {{ } else { }}
      throw AppError.validationError("validator.reference.undefined", { propertyPath });
    {{ } }}
  }

  return {{= reference }}(value, propertyPath);
};

// {{= JSON.stringify(it) }}
