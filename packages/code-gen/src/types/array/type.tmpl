{{ const result = typeExec({ type: item.values.type, item: item.values, isInputType, options }).trim(); }}
{{ if (item.validator.convert && it.isInputType) { }}
({{= result}}|{{= result }})[]
{{ } else { }}
({{= result}})[]
{{ } }}
