{{ const result = typeExec({ type: model.values.type, model: model.values, isInputType }).trim(); }}
{{ if (model.validator.convert) { }}
({{= result}}|{{= result }})[]
{{ } else { }}
({{= result}})[]
{{ } }}
