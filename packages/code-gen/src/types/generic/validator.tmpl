{{ const fnNumberKeys = ctx.anonFn(item.keys); }}
{{ const fnNumberValues = ctx.anonFn(item.values); }}

if (typeof value !== "object") {
  return buildError(`validator.${parentType}.type`, { propertyPath }, errorList);
}

const result = Object.create(null);
for (const key of Object.keys(value)) {
  const validatedKey = {{= item.keys.type }}Validator{{= fnNumberKeys }}(key, propertyPath + `.$key[${key}]`, errorList);

  result[validatedKey] = {{= item.values.type }}Validator{{= fnNumberValues }}(value[key], propertyPath + `.$value[${key}]`, errorList);
}

return result;
