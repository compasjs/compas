{{ const fnNumberKeys = ctx.anonFn(model.keys); }}
{{ const fnNumberValues = ctx.anonFn(model.values); }}

if (typeof value !== "object") {
  throw _errorFn(`validator.${parentType}.type`, { propertyPath });
}

const result = Object.create(null);
for (const key of Object.keys(value)) {
  const validatedKey = {{= model.keys.type }}Validator{{= fnNumberKeys }}(key, propertyPath + `.$key[${key}]`);
  const validatedValue = {{= model.values.type }}Validator{{= fnNumberValues }}(value[key], propertyPath + `.$value[${key}]`);

  result[validatedKey] = validatedValue;
}

return result;
