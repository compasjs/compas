{{ const keyMock = mockExec({ type: model.keys.type, model: model.keys, ignoreDefaults }); }}
{{ const valueMock = mockExec({ type: model.values.type, model: model.values, ignoreDefaults }); }}

Object.fromEntries(
{{= mockExec({ type: "array", model: { type: "array", validator: { min: 2, }, values: { mocks: { rawMock: `[${keyMock}, ${valueMock}],` } } }, ignoreDefaults }) }}
),
