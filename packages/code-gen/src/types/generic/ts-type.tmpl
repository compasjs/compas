{{ if (Array.isArray(item.keys?.oneOf)) { }}
{
[key in {{= typeExec({ type: item.keys.type, item: item.keys, isInputType, options }).trim() }}]:
{{= typeExec({ type: item.values.type, item: item.values, isInputType, options }).trim() }}
}
{{ } else { }}
{
[key: {{= typeExec({ type: item.keys.type, item: item.keys, isInputType, options }).trim() }}]:
{{= typeExec({ type: item.values.type, item: item.values, isInputType, options }).trim() }}
}
{{ } }}
