{{ if (Array.isArray(model.keys?.oneOf)) { }}
{
[key in {{= typeExec({ type: model.keys.type, model: model.keys, isInputType, options }).trim() }}]:
{{= typeExec({ type: model.values.type, model: model.values, isInputType, options }).trim() }}
}
{{ } else { }}
{
[key: {{= typeExec({ type: model.keys.type, model: model.keys, isInputType, options }).trim() }}]:
{{= typeExec({ type: model.values.type, model: model.values, isInputType, options }).trim() }}
}
{{ } }}
