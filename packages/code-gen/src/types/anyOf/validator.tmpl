const errors = [];

{{ for (const v of model.values) { }}
  try {
    {{ const fnNumber = ctx.anonFn(v); }}

    return {{= v.type }}Validator{{= fnNumber }}(value, propertyPath);
  } catch (e) {
    errors.push({
      name: e.name,
      message: e.message,
      key: e.key,
      info: e.info,
    });
  }
{{ } }}

throw _errorFn(`validator.${parentType}.type`, { propertyPath, errors });

