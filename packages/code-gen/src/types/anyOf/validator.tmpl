const errors = [];

{{ for (const v of model.values) { }}
  try {
    {{ const num = ctx.counter; }}
    {{ ctx.addFunc(validatorsAnonFn({ model: v, ctx })); }}

    return {{= v.type }}Validator{{= num }}(value, propertyPath);
  } catch (e) {
    errors.push({
      name: e.name,
      message: e.message,
      key: e.key,
      info: e.info,
    });
  }
{{ } }}

throw _errorFn("validator.anyOf.type", { propertyPath, errors });

