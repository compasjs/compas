{{ if (options.validatorCollectErrors) { }}
const { errors, data } = {{= lowerCaseFirst(model.reference.group) }}Validators.{{= lowerCaseFirst(model.reference.name) }}(value, propertyPath);
if (errors !== undefined) {
  errorList.push(...errors);
}
return data;
{{ } else { }}
return {{= lowerCaseFirst(model.reference.group) }}Validators.{{= lowerCaseFirst(model.reference.name) }}(value, propertyPath);
{{ } }}
