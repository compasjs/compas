{
{{ for (const key of Object.keys(item.keys)) { }}
  {{ const val = item.keys[key]; }}
  {{ let rightSide = typeExec({ type: val.type, item: val, isInputType, options }).trim(); }}
  {{ let separator = ":"; }}
  {{ if (rightSide.endsWith("|undefined")) { }}
    {{ rightSide = rightSide.substring(0, rightSide.length -10); }}
    {{ separator = "?:"; }}
  {{ } }}
  "{{= key }}"{{= separator }}{{= rightSide }},
{{ } }}
}
