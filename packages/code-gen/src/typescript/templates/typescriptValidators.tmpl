declare module "./validators" {
    export declare class ValidationError extends Error {
      static isValidationError(err: Error): err is ValidationError;

      constructor(key: string, message: string);
    }

    {{ for (const v of validators) { }}
    export type {{= v.typeName }} = {{= typescriptType(v.validator) }};
    export function {{= v.validatorName }}(value: unknown): {{= v.typeName }};
    {{ } }}
}
