/* eslint-disable no-unused-vars */
// Generated by @lbu/code-gen 2020-03-29T17:41:07.532Z

export const validatorHooks = {};

const isNil = (value) => value === undefined || value === null;

/**
 * This function should not throw as the corresponding validator will do that
 * @callback ValidationErrorFn
 * @param {string} key
 * @param {Object} info
 */

/**
 * @type ValidationErrorFn
 */
let _errorFn = (key, info) => {
  const err = new Error(`ValidationError: ${key}`);
  err.key = key;
  err.info = info;

  return err;
};

/**
 * Set a different error function, for example AppError.validationError
 * @param {ValidationErrorFn} fn
 */
export function validatorSetErrorFn(fn) {
  _errorFn = fn;
}

function booleanValidator13(value, propertyPath) {
  if (isNil(value)) {
    return false;
  }

  if (typeof value !== "boolean") {
    throw _errorFn("validator.boolean.type", { propertyPath });
  }

  return value;
}

function stringValidator14(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.string.undefined", { propertyPath });
  }

  if (typeof value !== "string") {
    throw _errorFn("validator.string.type", { propertyPath });
  }

  return value;
}

function objectValidator0(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.object.undefined", { propertyPath });
  }

  if (typeof value !== "object") {
    throw _errorFn("validator.object.type", { propertyPath });
  }
  const result = Object.create(null);

  result["completed"] = booleanValidator13(
    value["completed"],
    propertyPath + "." + "completed",
  );

  result["name"] = stringValidator14(
    value["name"],
    propertyPath + "." + "name",
  );

  return result;
}

/**
 *
 * @param {*} value
 * @returns { TodoItem}
 */
export function validateTodoItem(value) {
  let result = value;

  if ("preValidateTodoItem" in validatorHooks) {
    result = validatorHooks["preValidateTodoItem"](result);
  }

  result = objectValidator0(result, "$");

  if ("postValidateTodoItem" in validatorHooks) {
    return validatorHooks["postValidateTodoItem"](result);
  } else {
    return result;
  }
}

function stringValidator15(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.string.undefined", { propertyPath });
  }

  if (typeof value !== "string") {
    throw _errorFn("validator.string.type", { propertyPath });
  }

  return value;
}

function referenceValidator17(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.reference.undefined", { propertyPath });
  }

  return objectValidator0(value, propertyPath);
}

function arrayValidator16(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.array.undefined", { propertyPath });
  }

  if (!Array.isArray(value)) {
    throw _errorFn("validator.array.type", { propertyPath });
  }

  const result = [];

  for (let i = 0; i < value.length; ++i) {
    result.push(referenceValidator17(value[i], propertyPath + "[" + i + "]"));
  }

  return result;
}

function objectValidator1(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.object.undefined", { propertyPath });
  }

  if (typeof value !== "object") {
    throw _errorFn("validator.object.type", { propertyPath });
  }
  const result = Object.create(null);

  result["name"] = stringValidator15(
    value["name"],
    propertyPath + "." + "name",
  );

  result["items"] = arrayValidator16(
    value["items"],
    propertyPath + "." + "items",
  );

  return result;
}

/**
 *
 * @param {*} value
 * @returns { TodoList}
 */
export function validateTodoList(value) {
  let result = value;

  if ("preValidateTodoList" in validatorHooks) {
    result = validatorHooks["preValidateTodoList"](result);
  }

  result = objectValidator1(result, "$");

  if ("postValidateTodoList" in validatorHooks) {
    return validatorHooks["postValidateTodoList"](result);
  } else {
    return result;
  }
}

function referenceValidator18(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.reference.undefined", { propertyPath });
  }

  return stringValidator15(value, propertyPath);
}

function referenceValidator19(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.reference.undefined", { propertyPath });
  }

  return objectValidator1(value, propertyPath);
}

function genericValidator2(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.generic.undefined", { propertyPath });
  }
  if (typeof value !== "object") {
    throw _errorFn("validator.generic.type", { propertyPath });
  }

  const result = Object.create(null);
  for (const key of Object.keys(value)) {
    const validatedKey = referenceValidator18(key, propertyPath + ".$key");
    const validatedValue = referenceValidator19(
      value[key],
      propertyPath + ".$value",
    );

    result[validatedKey] = validatedValue;
  }

  return result;
}

/**
 *
 * @param {*} value
 * @returns { TodoCollection}
 */
export function validateTodoCollection(value) {
  let result = value;

  if ("preValidateTodoCollection" in validatorHooks) {
    result = validatorHooks["preValidateTodoCollection"](result);
  }

  result = genericValidator2(result, "$");

  if ("postValidateTodoCollection" in validatorHooks) {
    return validatorHooks["postValidateTodoCollection"](result);
  } else {
    return result;
  }
}

function referenceValidator20(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.reference.undefined", { propertyPath });
  }

  return genericValidator2(value, propertyPath);
}

function objectValidator3(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.object.undefined", { propertyPath });
  }

  if (typeof value !== "object") {
    throw _errorFn("validator.object.type", { propertyPath });
  }
  const result = Object.create(null);

  result["store"] = referenceValidator20(
    value["store"],
    propertyPath + "." + "store",
  );

  return result;
}

/**
 *
 * @param {*} value
 * @returns { TodoAllResponse}
 */
export function validateTodoAllResponse(value) {
  let result = value;

  if ("preValidateTodoAllResponse" in validatorHooks) {
    result = validatorHooks["preValidateTodoAllResponse"](result);
  }

  result = objectValidator3(result, "$");

  if ("postValidateTodoAllResponse" in validatorHooks) {
    return validatorHooks["postValidateTodoAllResponse"](result);
  } else {
    return result;
  }
}

function stringValidator21(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.string.undefined", { propertyPath });
  }

  if (typeof value !== "string") {
    value = String(value);
  }

  if (typeof value !== "string") {
    throw _errorFn("validator.string.type", { propertyPath });
  }

  value = value.trim();

  if (value.length < 0) {
    const min = 0;
    throw _errorFn("validator.string.min", { propertyPath, min });
  }

  if (value.length > 30) {
    const max = 30;
    throw _errorFn("validator.string.max", { propertyPath, max });
  }

  return value;
}

function objectValidator4(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.object.undefined", { propertyPath });
  }

  if (typeof value !== "object") {
    throw _errorFn("validator.object.type", { propertyPath });
  }
  const result = Object.create(null);

  result["name"] = stringValidator21(
    value["name"],
    propertyPath + "." + "name",
  );

  return result;
}

/**
 *
 * @param {*} value
 * @returns { TodoNameParam}
 */
export function validateTodoNameParam(value) {
  let result = value;

  if ("preValidateTodoNameParam" in validatorHooks) {
    result = validatorHooks["preValidateTodoNameParam"](result);
  }

  result = objectValidator4(result, "$");

  if ("postValidateTodoNameParam" in validatorHooks) {
    return validatorHooks["postValidateTodoNameParam"](result);
  } else {
    return result;
  }
}

function referenceValidator22(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.reference.undefined", { propertyPath });
  }

  return objectValidator1(value, propertyPath);
}

function objectValidator5(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.object.undefined", { propertyPath });
  }

  if (typeof value !== "object") {
    throw _errorFn("validator.object.type", { propertyPath });
  }
  const result = Object.create(null);

  result["todo"] = referenceValidator22(
    value["todo"],
    propertyPath + "." + "todo",
  );

  return result;
}

/**
 *
 * @param {*} value
 * @returns { TodoListResponse}
 */
export function validateTodoListResponse(value) {
  let result = value;

  if ("preValidateTodoListResponse" in validatorHooks) {
    result = validatorHooks["preValidateTodoListResponse"](result);
  }

  result = objectValidator5(result, "$");

  if ("postValidateTodoListResponse" in validatorHooks) {
    return validatorHooks["postValidateTodoListResponse"](result);
  } else {
    return result;
  }
}

function stringValidator23(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.string.undefined", { propertyPath });
  }

  if (typeof value !== "string") {
    throw _errorFn("validator.string.type", { propertyPath });
  }

  value = value.trim();

  if (value.length < 1) {
    const min = 1;
    throw _errorFn("validator.string.min", { propertyPath, min });
  }

  if (value.length > 40) {
    const max = 40;
    throw _errorFn("validator.string.max", { propertyPath, max });
  }

  return value;
}

function objectValidator6(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.object.undefined", { propertyPath });
  }

  if (typeof value !== "object") {
    throw _errorFn("validator.object.type", { propertyPath });
  }
  const result = Object.create(null);

  result["name"] = stringValidator23(
    value["name"],
    propertyPath + "." + "name",
  );

  return result;
}

/**
 *
 * @param {*} value
 * @returns { TodoNewBody}
 */
export function validateTodoNewBody(value) {
  let result = value;

  if ("preValidateTodoNewBody" in validatorHooks) {
    result = validatorHooks["preValidateTodoNewBody"](result);
  }

  result = objectValidator6(result, "$");

  if ("postValidateTodoNewBody" in validatorHooks) {
    return validatorHooks["postValidateTodoNewBody"](result);
  } else {
    return result;
  }
}

function stringValidator24(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.string.undefined", { propertyPath });
  }

  if (typeof value !== "string") {
    throw _errorFn("validator.string.type", { propertyPath });
  }

  value = value.trim();

  if (value.length < 1) {
    const min = 1;
    throw _errorFn("validator.string.min", { propertyPath, min });
  }

  if (value.length > 365) {
    const max = 365;
    throw _errorFn("validator.string.max", { propertyPath, max });
  }

  return value;
}

function objectValidator7(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.object.undefined", { propertyPath });
  }

  if (typeof value !== "object") {
    throw _errorFn("validator.object.type", { propertyPath });
  }
  const result = Object.create(null);

  result["name"] = stringValidator24(
    value["name"],
    propertyPath + "." + "name",
  );

  return result;
}

/**
 *
 * @param {*} value
 * @returns { TodoCreateItemBody}
 */
export function validateTodoCreateItemBody(value) {
  let result = value;

  if ("preValidateTodoCreateItemBody" in validatorHooks) {
    result = validatorHooks["preValidateTodoCreateItemBody"](result);
  }

  result = objectValidator7(result, "$");

  if ("postValidateTodoCreateItemBody" in validatorHooks) {
    return validatorHooks["postValidateTodoCreateItemBody"](result);
  } else {
    return result;
  }
}

function numberValidator25(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.number.undefined", { propertyPath });
  }

  if (typeof value !== "number") {
    value = Number(value);
  }

  if (typeof value !== "number" || isNaN(value) || !isFinite(value)) {
    throw _errorFn("validator.number.type", { propertyPath });
  }

  if (!Number.isInteger(value)) {
    throw _errorFn("validator.number.integer", { propertyPath });
  }

  if (value < 0) {
    const min = 0;
    throw _errorFn("validator.number.min", { propertyPath, min });
  }

  return value;
}

function objectValidator8(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.object.undefined", { propertyPath });
  }

  if (typeof value !== "object") {
    throw _errorFn("validator.object.type", { propertyPath });
  }
  const result = Object.create(null);

  result["index"] = numberValidator25(
    value["index"],
    propertyPath + "." + "index",
  );

  return result;
}

/**
 *
 * @param {*} value
 * @returns { TodoToggleItemBody}
 */
export function validateTodoToggleItemBody(value) {
  let result = value;

  if ("preValidateTodoToggleItemBody" in validatorHooks) {
    result = validatorHooks["preValidateTodoToggleItemBody"](result);
  }

  result = objectValidator8(result, "$");

  if ("postValidateTodoToggleItemBody" in validatorHooks) {
    return validatorHooks["postValidateTodoToggleItemBody"](result);
  } else {
    return result;
  }
}

function booleanValidator26(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.boolean.undefined", { propertyPath });
  }

  if (typeof value !== "boolean") {
    throw _errorFn("validator.boolean.type", { propertyPath });
  }

  return value;
}

function objectValidator9(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.object.undefined", { propertyPath });
  }

  if (typeof value !== "object") {
    throw _errorFn("validator.object.type", { propertyPath });
  }
  const result = Object.create(null);

  result["deleted"] = booleanValidator26(
    value["deleted"],
    propertyPath + "." + "deleted",
  );

  return result;
}

/**
 *
 * @param {*} value
 * @returns { TodoDeleteResponse}
 */
export function validateTodoDeleteResponse(value) {
  let result = value;

  if ("preValidateTodoDeleteResponse" in validatorHooks) {
    result = validatorHooks["preValidateTodoDeleteResponse"](result);
  }

  result = objectValidator9(result, "$");

  if ("postValidateTodoDeleteResponse" in validatorHooks) {
    return validatorHooks["postValidateTodoDeleteResponse"](result);
  } else {
    return result;
  }
}

function stringValidator27(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.string.undefined", { propertyPath });
  }

  if (typeof value !== "string") {
    throw _errorFn("validator.string.type", { propertyPath });
  }

  return value;
}

function stringValidator28(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.string.undefined", { propertyPath });
  }

  if (typeof value !== "string") {
    throw _errorFn("validator.string.type", { propertyPath });
  }

  return value;
}

function numberValidator29(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.number.undefined", { propertyPath });
  }

  if (typeof value !== "number" || isNaN(value) || !isFinite(value)) {
    throw _errorFn("validator.number.type", { propertyPath });
  }

  if (!Number.isInteger(value)) {
    throw _errorFn("validator.number.integer", { propertyPath });
  }

  return value;
}

function objectValidator10(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.object.undefined", { propertyPath });
  }

  if (typeof value !== "object") {
    throw _errorFn("validator.object.type", { propertyPath });
  }
  const result = Object.create(null);

  result["id"] = stringValidator27(value["id"], propertyPath + "." + "id");

  result["name"] = stringValidator28(
    value["name"],
    propertyPath + "." + "name",
  );

  result["age"] = numberValidator29(value["age"], propertyPath + "." + "age");

  return result;
}

/**
 *
 * @param {*} value
 * @returns { User}
 */
export function validateUser(value) {
  let result = value;

  if ("preValidateUser" in validatorHooks) {
    result = validatorHooks["preValidateUser"](result);
  }

  result = objectValidator10(result, "$");

  if ("postValidateUser" in validatorHooks) {
    return validatorHooks["postValidateUser"](result);
  } else {
    return result;
  }
}

function stringValidator11(value, propertyPath) {
  if (isNil(value)) {
    return "NORTH";
  }

  if (typeof value !== "string") {
    throw _errorFn("validator.string.type", { propertyPath });
  }

  if (
    value !== "NORTH" &&
    value !== "EAST" &&
    value !== "SOUTH" &&
    value !== "WEST"
  ) {
    const oneOf = "NORTH, EAST, SOUTH, WEST";
    throw _errorFn("validator.string.oneOf", { propertyPath, oneOf });
  }

  return value;
}

/**
 *
 * @param {*} value
 * @returns { WindDirection}
 */
export function validateWindDirection(value) {
  let result = value;

  if ("preValidateWindDirection" in validatorHooks) {
    result = validatorHooks["preValidateWindDirection"](result);
  }

  result = stringValidator11(result, "$");

  if ("postValidateWindDirection" in validatorHooks) {
    return validatorHooks["postValidateWindDirection"](result);
  } else {
    return result;
  }
}

function booleanValidator30(value, propertyPath) {
  if (isNil(value)) {
    return true;
  }

  if (typeof value !== "boolean") {
    throw _errorFn("validator.boolean.type", { propertyPath });
  }

  return value;
}

function numberValidator31(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.number.undefined", { propertyPath });
  }

  if (typeof value !== "number") {
    value = Number(value);
  }

  if (typeof value !== "number" || isNaN(value) || !isFinite(value)) {
    throw _errorFn("validator.number.type", { propertyPath });
  }

  if (!Number.isInteger(value)) {
    throw _errorFn("validator.number.integer", { propertyPath });
  }

  if (value < 0) {
    const min = 0;
    throw _errorFn("validator.number.min", { propertyPath, min });
  }

  if (value > 10) {
    const max = 10;
    throw _errorFn("validator.number.max", { propertyPath, max });
  }

  return value;
}

function referenceValidator32(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.reference.undefined", { propertyPath });
  }

  return stringValidator11(value, propertyPath);
}

function numberValidator35(value, propertyPath) {
  if (isNil(value)) {
    return;
  }

  if (typeof value !== "number" || isNaN(value) || !isFinite(value)) {
    throw _errorFn("validator.number.type", { propertyPath });
  }

  if (value < 1) {
    const min = 1;
    throw _errorFn("validator.number.min", { propertyPath, min });
  }

  if (value > 150) {
    const max = 150;
    throw _errorFn("validator.number.max", { propertyPath, max });
  }

  return value;
}

function stringValidator36(value, propertyPath) {
  if (isNil(value)) {
    return new Date().toISOString();
  }

  if (typeof value !== "string") {
    throw _errorFn("validator.string.type", { propertyPath });
  }

  return value;
}

function booleanValidator38(value, propertyPath) {
  if (isNil(value)) {
    return false;
  }

  if (typeof value !== "boolean") {
    if (value === "true" || value === 1) {
      value = true;
    } else if (value === "false" || value === 0) {
      value = false;
    }
  }

  if (typeof value !== "boolean") {
    throw _errorFn("validator.boolean.type", { propertyPath });
  }

  return value;
}

function arrayValidator37(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.array.undefined", { propertyPath });
  }

  if (!Array.isArray(value)) {
    throw _errorFn("validator.array.type", { propertyPath });
  }

  const result = [];

  for (let i = 0; i < value.length; ++i) {
    result.push(booleanValidator38(value[i], propertyPath + "[" + i + "]"));
  }

  return result;
}

function booleanValidator40(value, propertyPath) {
  if (isNil(value)) {
    return;
  }

  if (typeof value !== "boolean") {
    throw _errorFn("validator.boolean.type", { propertyPath });
  }

  return value;
}

function objectValidator39(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.object.undefined", { propertyPath });
  }

  if (typeof value !== "object") {
    throw _errorFn("validator.object.type", { propertyPath });
  }
  const result = Object.create(null);

  result["foo"] = booleanValidator40(value["foo"], propertyPath + "." + "foo");

  return result;
}

function anyOfValidator34(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.anyOf.undefined", { propertyPath });
  }

  const errors = [];

  try {
    return numberValidator35(value, propertyPath);
  } catch (e) {
    errors.push({
      name: e.name,
      message: e.message,
      key: e.key,
      info: e.info,
    });
  }

  try {
    return stringValidator36(value, propertyPath);
  } catch (e) {
    errors.push({
      name: e.name,
      message: e.message,
      key: e.key,
      info: e.info,
    });
  }

  try {
    return arrayValidator37(value, propertyPath);
  } catch (e) {
    errors.push({
      name: e.name,
      message: e.message,
      key: e.key,
      info: e.info,
    });
  }

  try {
    return objectValidator39(value, propertyPath);
  } catch (e) {
    errors.push({
      name: e.name,
      message: e.message,
      key: e.key,
      info: e.info,
    });
  }

  throw _errorFn("validator.anyOf.type", { propertyPath, errors });
}

function arrayValidator33(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.array.undefined", { propertyPath });
  }

  if (!Array.isArray(value)) {
    throw _errorFn("validator.array.type", { propertyPath });
  }

  const result = [];

  for (let i = 0; i < value.length; ++i) {
    result.push(anyOfValidator34(value[i], propertyPath + "[" + i + "]"));
  }

  return result;
}

function objectValidator12(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.object.undefined", { propertyPath });
  }

  if (typeof value !== "object") {
    throw _errorFn("validator.object.type", { propertyPath });
  }
  const result = Object.create(null);

  result["darkMode"] = booleanValidator30(
    value["darkMode"],
    propertyPath + "." + "darkMode",
  );

  result["preferredNumber"] = numberValidator31(
    value["preferredNumber"],
    propertyPath + "." + "preferredNumber",
  );

  result["direction"] = referenceValidator32(
    value["direction"],
    propertyPath + "." + "direction",
  );

  result["totalMess"] = arrayValidator33(
    value["totalMess"],
    propertyPath + "." + "totalMess",
  );

  return result;
}

/**
 *
 * @param {*} value
 * @returns { UnimplementedSettingsResponse}
 */
export function validateUnimplementedSettingsResponse(value) {
  let result = value;

  if ("preValidateUnimplementedSettingsResponse" in validatorHooks) {
    result = validatorHooks["preValidateUnimplementedSettingsResponse"](result);
  }

  result = objectValidator12(result, "$");

  if ("postValidateUnimplementedSettingsResponse" in validatorHooks) {
    return validatorHooks["postValidateUnimplementedSettingsResponse"](result);
  } else {
    return result;
  }
}
