{
  "name": "lbu",
  "private": true,
  "scripts": {
    "build": "tsc -b",
    "lint": "yarn eslint --fix && yarn prettier --write",
    "prettier": "prettier -l '*/**/**.{js,ts,json}'",
    "eslint": "eslint ./packages/*/src/**/*.ts",
    "test": "jest",
    "bump": "lerna changed && lerna version",
    "publish": "yarn build && lerna publish"
  },
  "devDependencies": {
    "@lbu/lint-config": "0.0.1",
    "@types/jest": "24.0.25",
    "jest": "24.9.0",
    "lerna": "3.20.1",
    "ts-jest": "24.2.0",
    "typescript": "3.7.4"
  },
  "workspaces": [
    "packages/*"
  ]
}
