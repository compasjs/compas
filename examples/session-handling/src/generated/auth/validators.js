// Generated by @compas/code-gen
/* eslint-disable no-unused-vars */

import {
  anonymousValidator1027736033,
  anonymousValidator1221278603,
  anonymousValidator1347962962,
  anonymousValidator1649918931,
  anonymousValidator592157009,
} from "../common/anonymous-validators.js";
import { AppError, isNil } from "@compas/stdlib";
/**
 * @template T
 * @typedef {import("@compas/stdlib").Either<T, AppError>} Either
 */
/**
 * @param {undefined|any|AuthLogoutResponseInput} value
 * @param {string|undefined} [propertyPath]
 * @returns {Either<AuthLogoutResponse>}
 */
export function validateAuthLogoutResponse(value, propertyPath = "$") {
  const result = anonymousValidator1649918931(value, propertyPath);
  if (result.errors) {
    const info = {};
    for (const err of result.errors) {
      if (isNil(info[err.propertyPath])) {
        info[err.propertyPath] = err;
      } else if (Array.isArray(info[err.propertyPath])) {
        info[err.propertyPath].push(err);
      } else {
        info[err.propertyPath] = [info[err.propertyPath], err];
      }
    }
    /** @type {{ error: AppError }} */
    return {
      error: AppError.validationError("validator.error", info),
    };
  }
  /** @type {{ value: AuthLogoutResponse}} */
  return { value: result.value };
}
/**
 * @param {undefined|any|AuthMeResponseInput} value
 * @param {string|undefined} [propertyPath]
 * @returns {Either<AuthMeResponse>}
 */
export function validateAuthMeResponse(value, propertyPath = "$") {
  const result = anonymousValidator1027736033(value, propertyPath);
  if (result.errors) {
    const info = {};
    for (const err of result.errors) {
      if (isNil(info[err.propertyPath])) {
        info[err.propertyPath] = err;
      } else if (Array.isArray(info[err.propertyPath])) {
        info[err.propertyPath].push(err);
      } else {
        info[err.propertyPath] = [info[err.propertyPath], err];
      }
    }
    /** @type {{ error: AppError }} */
    return {
      error: AppError.validationError("validator.error", info),
    };
  }
  /** @type {{ value: AuthMeResponse}} */
  return { value: result.value };
}
/**
 * @param {undefined|any|AuthRefreshTokensBodyInput} value
 * @param {string|undefined} [propertyPath]
 * @returns {Either<AuthRefreshTokensBody>}
 */
export function validateAuthRefreshTokensBody(value, propertyPath = "$") {
  const result = anonymousValidator592157009(value, propertyPath);
  if (result.errors) {
    const info = {};
    for (const err of result.errors) {
      if (isNil(info[err.propertyPath])) {
        info[err.propertyPath] = err;
      } else if (Array.isArray(info[err.propertyPath])) {
        info[err.propertyPath].push(err);
      } else {
        info[err.propertyPath] = [info[err.propertyPath], err];
      }
    }
    /** @type {{ error: AppError }} */
    return {
      error: AppError.validationError("validator.error", info),
    };
  }
  /** @type {{ value: AuthRefreshTokensBody}} */
  return { value: result.value };
}
/**
 * @param {undefined|any|AuthSessionInput} value
 * @param {string|undefined} [propertyPath]
 * @returns {Either<AuthSession>}
 */
export function validateAuthSession(value, propertyPath = "$") {
  const result = anonymousValidator1347962962(value, propertyPath);
  if (result.errors) {
    const info = {};
    for (const err of result.errors) {
      if (isNil(info[err.propertyPath])) {
        info[err.propertyPath] = err;
      } else if (Array.isArray(info[err.propertyPath])) {
        info[err.propertyPath].push(err);
      } else {
        info[err.propertyPath] = [info[err.propertyPath], err];
      }
    }
    /** @type {{ error: AppError }} */
    return {
      error: AppError.validationError("validator.error", info),
    };
  }
  /** @type {{ value: AuthSession}} */
  return { value: result.value };
}
/**
 * @param {undefined|any|AuthTokenPairInput} value
 * @param {string|undefined} [propertyPath]
 * @returns {Either<AuthTokenPair>}
 */
export function validateAuthTokenPair(value, propertyPath = "$") {
  const result = anonymousValidator1221278603(value, propertyPath);
  if (result.errors) {
    const info = {};
    for (const err of result.errors) {
      if (isNil(info[err.propertyPath])) {
        info[err.propertyPath] = err;
      } else if (Array.isArray(info[err.propertyPath])) {
        info[err.propertyPath].push(err);
      } else {
        info[err.propertyPath] = [info[err.propertyPath], err];
      }
    }
    /** @type {{ error: AppError }} */
    return {
      error: AppError.validationError("validator.error", info),
    };
  }
  /** @type {{ value: AuthTokenPair}} */
  return { value: result.value };
}
